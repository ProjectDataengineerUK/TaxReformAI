# TaxReform AI

> Plataforma SaaS B2B Enterprise de Inteligência Tributária e Compliance em Tempo Real, para apoiar departamentos fiscais, controllers, CFOs e consultorias tributárias durante a transição do modelo tributário brasileiro (PIS, COFINS, IPI, ICMS, ISS) para o novo IVA Dual (CBS, IBS e Imposto Seletivo). Combina RAG Híbrido com AST de legislação, regras determinísticas de cálculo e guardrails contábeis em sandbox Python, garantindo simulações 100% auditáveis com citação de fontes oficiais.

---

## Status do projeto

`contexto.md` continua sendo o blueprint completo (produto, arquitetura multi-agente, infraestrutura GCP, modelagem de dados, API e modelo de negócio) que orienta a implementação. Doze features já foram construídas seguindo o workflow SDD (`/brainstorm` → `/define` → `/design` → `/build` → `/ship`), documentadas em `.claude/sdd/`:

| Feature | Status | O que faz |
|---------|--------|-----------|
| `PIPELINE_INGESTAO_LEGAL` (`ingestion/`) | ✅ Shipado (`.claude/sdd/archive/PIPELINE_INGESTAO_LEGAL/`) — `GCP_PROJECT_ID`/`GCS_BUCKET_NAME` já são reais (bucket `taxreformai-dev-legal-docs` provisionado via Terraform); `QDRANT_URL`/`QDRANT_API_KEY` também já configurados (Qdrant Cloud free tier), todos em GitHub Secrets. Busca híbrida real ainda não verificada E2E — só roda de fato via `dags/ingestao_legal_dag.py` num Cloud Composer real, pendente de provisionamento | Raspa o Planalto, estrutura a legislação em árvore AST (Lei→Título→Capítulo→Artigo→Parágrafo→Inciso→Alínea), gera chunks parent-child e indexa via embedding híbrido (BGE-M3+BM25) no Qdrant |
| `INGESTAO_TCU_E_ETL_AIRFLOW` (`ingestion/scraper/tcu_scraper.py`, `ingestion/parser/resolucao_parser.py`, `dags/`) | ✅ Shipado (`.claude/sdd/archive/INGESTAO_TCU_E_ETL_AIRFLOW/`) | Segunda fonte real (Resoluções TCU em PDF, via `pdftotext`) provando que `LegalSource`/`Lei.artigos_soltos`/`chunker.py` já eram genéricos o bastante sem modificação; `dags/ingestao_legal_dag.py` escreve a orquestração real do Airflow/Cloud Composer (TaskFlow API) que substitui a CLI — `apache-airflow` não instala neste sandbox, então a DAG é validada por revisão de código, não por teste automatizado (execução real pendente de Cloud Composer provisionado, decisão de negócio fora de escopo) |
| `MOTOR_DETERMINISTICO_CALCULO` (`motor_calculo/`) | ✅ Shipado (`.claude/sdd/archive/MOTOR_DETERMINISTICO_CALCULO/`) | Calcula CBS/IBS/IS/Split Payment por fase da transição (2026-2033), com alíquotas rastreáveis ao artigo da LCP 214/2025. 2026 completo (CBS 0,9% art. 346 + IBS 0,1% art. 343); 2027-2028 **parcial** — o art. 344 fixa o IBS (0,05% estadual + 0,05% municipal), mas o art. 347 deixa a CBS dependente de alíquota de referência ainda não fixada, e o IS é fixado por lei ordinária por produto. Alíquota não fixada é `None`, nunca estimada: a recusa nomeia o tributo faltante e o dispositivo que o rege |
| `ORQUESTRACAO_MULTIAGENTE` (`orquestracao/`) | ✅ Shipado (`.claude/sdd/archive/ORQUESTRACAO_MULTIAGENTE/`) — **4 dos 5 nós tornados reais por `LLM_REAL_VERTEX_AI`, ver linha própria** | Grafo LangGraph com os 5 agentes do blueprint (Classificador→Pesquisador Legal→Extrator→Determinístico→Sintetizador); `langgraph` não instalável neste sandbox — wiring isolado e não exercitado, ver BUILD_REPORT |
| `API_HTTP_SIMULACAO` (`api/`) | ✅ Shipado (`.claude/sdd/archive/API_HTTP_SIMULACAO/`) | FastAPI com `POST /v1/tax/simulate` (estruturado, schema da seção 8, integração ERP) e `POST /v1/tax/query` (conversacional, expõe `orquestracao/` via novo `orquestracao/executor.py` — sequencial, sem depender de `langgraph`); auth via `X-API-Key`. Primeira feature sem nenhum blocker de dependência; testada com `uvicorn` real + `curl`, não só testes automatizados |
| `FRONTEND_SIMULADOR` (`frontend/`) | ✅ Shipado (`.claude/sdd/archive/FRONTEND_SIMULADOR/`) | Next.js 14 (App Router) com `/simulador` (formulário estruturado) e `/consulta` (conversacional), consumindo a API real; API key via `localStorage`. Corrigiu de quebra um bug crítico de CORS em `api/main.py` (a API não tinha `CORSMiddleware` — nenhuma chamada de um navegador real teria funcionado) |

| `SCHEMA_POSTGRESQL` (`db/`, `motor_calculo/regime_atual.py`) | ✅ Shipado (`.claude/sdd/archive/SCHEMA_POSTGRESQL/`) | Schema da seção 7 (multi-tenancy via RLS, audit log, cache de regras) **aplicado no Cloud SQL real** e conectado à API deployada — audit log gravando de verdade, provado por consulta separada. Mais o regime tributário vigente (PIS/COFINS, ICMS interestadual) com todas as alíquotas citadas por artigo real do Planalto/LexML, nunca de memória |
| `IPI_TIPI_MOTOR_CALCULO` (`api/ipi.py`, `db/repositorio.py`, `api/routers/simulate.py`) | ✅ Shipado (`.claude/sdd/archive/IPI_TIPI_MOTOR_CALCULO/`) — duas verificações reais: `migrar_banco.yml` com `verificar_ipi=sim` (run `30299629790`) provou que o papel de runtime `taxreformai_app` lê `aliquotas_ipi_tipi` (9231 linhas), distinguindo alíquota 0% real de "NT"; `deploy.yml` (run `30300917434`) confirmou `total_ipi=3.90` no smoke test contra a API pública | Liga a tabela `aliquotas_ipi_tipi` (9231 NCMs já ingeridos) ao `/v1/tax/simulate`: lookup em lote (1 query por request, `= ANY(%s)`), IPI por item com o `dispositivo_legal_ref` da própria linha como fonte. `ipi_situacao` é um enum de 5 estados — NT nunca vira 0%, NCM ausente nunca vira omissão, banco fora do ar nunca vira 5xx. Primeira das 11 features de `.claude/sdd/features/ROADMAP_SEQUENCIA_AUDITORIA_2026-07.md` a ser shipada |

| `REGRAS_TRIBUTARIAS_CACHE` (`db/migrations/005`+`006`, `api/cesta_basica.py`, `api/ncm.py`, `motor_calculo/reducoes.py`) | ✅ Shipado (`.claude/sdd/archive/REGRAS_TRIBUTARIAS_CACHE/`) — duas verificações reais: `migrar_banco.yml` com `verificar_cesta_basica=sim` (run `30383959322`) provou que o papel de runtime lê o Anexo I (26 itens, 76 inclusões, 19 exceções) e confirmou os 3 mecanismos — correspondência exata (manteiga, `04051000`), por prefixo/posição (`09012100`) e exclusão (`02074300`, excluído pelo item 19); `deploy.yml` (run `30384142935`) confirmou `situacao=APLICADA, cbs dispensado=9.00` no smoke test contra a API pública | Cesta Básica Nacional (LCP 214/2025, art. 125 e Anexo I, 26 itens transcritos do DOU): `/v1/tax/simulate` aplica alíquota **zero** de CBS/IBS por item, citando "art. 125, Anexo I, item N". Toda correspondência por NCM é prefixo de dígitos de 4 a 8 ("exato" é o prefixo de 8), o que resolve os 26 itens com um só mecanismo, inclusive as 19 exceções dos itens 19/20 (foie gras, salmonídeos, atuns). Remove `regras_tributarias_cache`/`buscar_regra_cache()` — código morto desde `SCHEMA_POSTGRESQL`. Degradação **conservadora**: falha de infra aplica a alíquota geral (nunca zero indevido) — segunda das 11 features de `ROADMAP_SEQUENCIA_AUDITORIA_2026-07.md` |

| `ANEXOS_REDUCAO_ZERO_XII_XIII_XV` (`db/migrations/007`+`008`) | ✅ Shipado (`.claude/sdd/archive/ANEXOS_REDUCAO_ZERO_XII_XIII_XV/`) — duas verificações reais: `migrar_banco.yml` (run `30445357996`) provou 7 casos, incluindo o desempate de 3 vias do Anexo XII e o prefixo de 2 dígitos do Anexo XV, sem regressão no Anexo I; `deploy.yml` (run `30446862421`) confirmou o Capítulo 6 no smoke test contra a API pública | Generalizou o schema do Anexo I para os **4 Anexos de redução a ZERO** da LCP 214/2025: XII (art. 144, dispositivos médicos), XIII (art. 145, acessibilidade) e XV (art. 148, hortícolas/frutas/ovos). Prefixo passa a aceitar **capítulo (2 dígitos)**. Bloco da resposta renomeado de `cesta_basica` para `reducao_zero` (depois generalizado de novo, ver linha abaixo) — 12ª posição do roadmap, primeira da "segunda leva" |
| `ANEXOS_REDUCAO_PERCENTUAL_NCM` (`db/migrations/009`+`010`, `api/reducao.py`, `motor_calculo/reducoes.py`) | ✅ Shipado (`.claude/sdd/archive/ANEXOS_REDUCAO_PERCENTUAL_NCM/`) — duas verificações reais: `migrar_banco.yml` com `verificar_reducao=sim` (run `30544008270`) provou 13 casos contra o Cloud SQL, incluindo a precedência normativa Anexo I/XV sobre o VII, o multi-capítulo do Anexo IX e a condição de comprador dos Anexos IV/V/VI, sem regressão nos 4 Anexos zero herdados; `deploy.yml` (run `30544203775`) confirmou `cbs_percentual=0.36` (Anexo VIII, item 1, sabão de toucador) no smoke test contra a API pública, mais as 3 chamadas anteriores (IPI, Cesta Básica, Capítulo 6) sem regressão | Generaliza de novo o schema (agora **10 Anexos**: os 4 a zero + IV/V/VI/VII/VIII/IX a **60%**) e introduz `aplicar_reducao_percentual` — primeira redução fracionária do projeto, reduz a ALÍQUOTA (não o valor arredondado). Achado do `/design` que refutou uma premissa do `/define`: **117 pares de sobreposição** de NCM entre os Anexos novos e os 4 já shipados, 35 deles no MESMO código de 8 dígitos — sem generalizar o desempate para 6 componentes, 60% venceria zero em 35 casos. Novo campo de payload `comprador_tipo` (`ORGAO_PUBLICO`/`ENTIDADE_CEBAS_SUS`) resolve a condição real dos Anexos IV/V/VI (arts. 144-II/145-II/146§2º — zero quando o comprador é órgão público/CEBAS). Bloco da resposta renomeado de `reducao_zero` para `reducao`, sem alias — 13ª posição do roadmap |
| `ANEXOS_REDUCAO_PERCENTUAL_NBS` (`db/migrations/011`+`012`, `api/nbs.py`, `api/reducao_nbs.py`) | ✅ Shipado, **completo** (`.claude/sdd/archive/ANEXOS_REDUCAO_PERCENTUAL_NBS/`) — Anexo X fechado no mesmo dia via continuação pós-ship (migração 012), depois que uma rota alternativa (PDF da Câmara dos Deputados + `pdftotext`, contornando o limite de tamanho da ferramenta de leitura web) conseguiu ler o art. 139 por completo; duas verificações reais: `migrar_banco.yml` com `verificar_reducao_nbs=sim` (run `30634605300` para II/III/XI, ver SHIPPED para o run da 012) provou os 4 Anexos contra o Cloud SQL com o papel de runtime; `deploy.yml` (run `30634735254`) confirmou `cbs_percentual=0.36` (Anexo II, item 4) no smoke test contra a API pública | Primeiro vocabulário do projeto por **NBS** (Nomenclatura Brasileira de Serviços, 9 dígitos, não NCM/SH): os **4 Anexos completos** — II (Educação), III (Saúde), X (Produções artísticas/culturais/audiovisuais, 47 itens) e XI (Segurança/cibersegurança) —, com tabelas PRÓPRIAS (`anexos_reducao_nbs`/`anexos_reducao_nbs_prefixo`, nunca comingladas com as tabelas NCM) e situação nova `CONDICAO_NAO_SATISFEITA` — o Anexo XI inverte a polaridade de IV/V/VI (condição HABILITA os 60% a partir da alíquota geral, não os reduz a partir de 60%) e o Anexo X usa o MESMO mecanismo para a condição de nacionalidade de conteúdo (art. 139, §1º), com o mapeamento item-a-inciso documentado como leitura de boa-fé (não jurisprudência) na migração 012 — 14ª posição do roadmap |
| `ANEXO_XVI_PISO_ALIQUOTA_PROPRIA` (`motor_calculo/piso_aliquota_ibs.py`) | ✅ Shipado (`.claude/sdd/archive/ANEXO_XVI_PISO_ALIQUOTA_PROPRIA/`) — primeira feature do projeto SEM nenhuma infraestrutura: Python puro, sem migração, sem Cloud SQL, sem `GRANT`, sem workflow — verificação é só `pytest`/`ruff`, já 100% local | Piso do art. 371/Anexo XVI da LCP 214/2025 (49 anos, 2029-2077) — o percentual mínimo que Estados/Municípios podem fixar para sua fatia do IBS, em proporção de uma alíquota de referência que este projeto NUNCA calcula (é grandeza derivada de execução fiscal real, art. 370 — bloqueio estrutural permanente, diferente do CBS de referência 2027-2028, que é só lei ainda não promulgada). **Achado real do `/build`**: `/v1/tax/simulate` já devolve 422 para QUALQUER `ano_operacao >= 2027` (só 2026 responde 200 hoje) — a interseção com a janela do Anexo XVI (2029-2077) é vazia, então o campo embutido planejado (`RespostaSimulacao.piso_aliquota_ibs`, mantido, forward-compatible) nunca apareceria em nenhuma resposta real; resolvido com um endpoint dedicado, `GET /v1/tax/piso-aliquota-ibs/{ano_operacao}`, que não depende do motor de cálculo do IVA Dual — 15ª posição do roadmap |
| `ANEXO_XVII_IMPOSTO_SELETIVO_INCIDENCIA` (`db/migrations/013`, `api/imposto_seletivo.py`) | ✅ Shipado (`.claude/sdd/archive/ANEXO_XVII_IMPOSTO_SELETIVO_INCIDENCIA/`) — sem nenhum desvio do `/design`, primeira feature da leva com esse resultado | Primeiro tributo do projeto que NÃO é CBS nem IBS: base de incidência do Imposto Seletivo (LCP 214/2025, art. 409, §§1º-2º, Anexo XVII) — 6 categorias com código NCM (veículos, aeronaves/embarcações, produtos fumígenos, bebidas alcoólicas, bebidas açucaradas, bens minerais), a 7ª (concursos de prognósticos e fantasy sport) sem código citável, nunca inserida. NUNCA calcula valor de IS — `motor_calculo/tabela_aliquotas.py` intocado, `ImpostoSeletivoItem` sem nenhum campo de valor/percentual por desenho (mesma disciplina do Anexo XVI). Achado do `/define`: produtos fumígenos/bebidas alcoólicas só entram na base "quando acondicionados em embalagem primária... destinada ao consumidor final" (art. 409, §2º) — condição de GATING reaproveitando o nome `CONDICAO_NAO_SATISFEITA` já usado em `api/reducao_nbs.py`. A exceção de uso militar/Segurança Pública (veículos/aeronaves) nunca é verificada, mas sempre declarada — 16ª posição do roadmap |
| `SIMPLES_NACIONAL_CBS_IBS_TRANSICAO` (`motor_calculo/simples_nacional.py`, `api/schemas_simples_nacional.py`) | ✅ Shipado (`.claude/sdd/archive/SIMPLES_NACIONAL_CBS_IBS_TRANSICAO/`) — sem nenhum desvio do `/design` | Integra CBS/IBS à partilha do Simples Nacional (LCP 214/2025, Anexos XVIII-XXIII = Anexos I/II/III/IV/V/VII da LC 123/2006) via endpoint dedicado `POST /v1/tax/simulate-simples-nacional`, independente de `engine.py`/`TabelaAliquotasSeed`. Achado central: a fórmula de cálculo (alíquota efetiva, LC 123/2006 art. 18, §§1º-A/1º-B) não está nos Anexos da LCP 214/2025 — é a primeira feature do projeto com uma fórmula real, não só lookup de tabela. Achado do `/design`, mesma classe do Anexo XVI: `/v1/tax/simulate` já 422 para todo `ano_operacao >= 2027`, exigindo o endpoint dedicado. Módulo Python puro (`motor_calculo/simples_nacional.py`) com os 6 Anexos completos (faixas + partilha ano a ano até 2033 permanente + teto de ISS com coeficientes por ano + MEI) — maior volume de dado legal verificado de toda a leva — 17ª e última posição do roadmap, "segunda leva" 100% completa |
| `API_EMPRESA_SKUS` (`api/routers/empresa_skus.py`, `db/migrations/014`) | ✅ Shipado (`.claude/sdd/archive/API_EMPRESA_SKUS/`) — 2 desvios do `/design` corrigidos no build, mais revisão de segurança dedicada (5 achados, 3 corrigidos) | Primeira feature CRUD com ESCRITA multi-tenant desde `SCHEMA_POSTGRESQL` — toda feature anterior só lia tabela pública de referência. CRUD completo + upload CSV síncrono (`/v1/tax/skus`) sobre a tabela `empresa_skus` (schema/RLS aplicados desde 2026-07-27, nunca antes lida/escrita). Migração 014 corrige um gap real: `ncm_code` era `NOT NULL` sem coluna `natureza`, forçando NCM inventado em SKU de serviço — agora `natureza` (mesmo vocabulário de `ItemSimulacao`) mais `CHECK` de exclusividade NCM xor NBS. `/v1/tax/simulate` passa a resolver `ncm`/`nbs` a partir do `sku` cadastrado quando ambos ausentes do item — explícito sempre vence. Achado do `/build`: "SERVIÇO sem `nbs`" já era estado válido ANTES desta feature (nunca erro) — o 422 de "SKU não resolvido" ficou restrito a MERCADORIA, a única natureza que ganhou uma lacuna nova. Primeira posição retomada da "primeira leva" depois da "segunda leva" 100% completa |
| `LLM_REAL_VERTEX_AI` (`orquestracao/llm/`, `orquestracao/dependencias.py`, `api/dependencias_orquestracao.py`) | ✅ Shipado (`.claude/sdd/archive/LLM_REAL_VERTEX_AI/`) — verificado contra infraestrutura real em 3 dispatches (Terraform + `deploy.yml`), achou e corrigiu 2 bugs reais de infra (OOM de memória, IAM de logging); bloqueado no caminho 200 só por quota do Vertex AI no Console do GCP (ação externa do usuário, não bug) | Fundiu as posições 4 e 5 do roadmap por instrução explícita do usuário ("ja pode entregar tudo"): conecta Claude via Vertex AI de verdade (`anthropic[vertex]`, client `AnthropicVertex`, endpoint `region="global"` — recomendação oficial da Anthropic, sem premium de preço, elimina o conflito com `southamerica-east1`) E reescreve os 4 nós fake da orquestração (`classificador`→Haiku, `pesquisador_legal`/`extrator_regras`/`sintetizador`→Sonnet); `deterministico` continua intocado. `pesquisador_legal` reusa `QdrantIndexer.search_hybrid` já verificado em produção desde `PIPELINE_INGESTAO_LEGAL` — nenhuma reingestão. Achado real do `/build`: `extrator_regras` extrai de `texto_consulta` via LLM mas os campos estruturados do payload (`valor_base`/`ano_operacao`) SEMPRE vencem para o cálculo — a extração só registra divergência no histórico auditável, nunca altera o que alimenta `motor_calculo`. `sintetizador` tem um guardrail que rejeita (503) qualquer parecer que não reproduza literalmente TODOS os valores calculados (CBS/IBS/IS/valor_base/valor_liquido) e a `fonte_legal` citada. **Revisão de segurança dedicada** encontrou 2 achados CRÍTICOS (regex de mascaramento de PII com separador literal fixo deixava passar CPF/CNPJ com espaço/travessão/separador trocado; guardrail do sintetizador só checava `valor_liquido`, permitindo CBS/IBS/IS/fonte_legal alterados) e 2 ALTOS (audit log gravava o texto BRUTO, não o mascarado; fallback silencioso `texto_mascarado or texto_consulta` sem assert) — todos corrigidos na mesma sessão, antes do `/ship`. `taxreformai-runtime` ganha `roles/aiplatform.user` — primeira role de PROJETO dessa SA, desvio documentado e escopado do princípio "zero role" de `SCHEMA_POSTGRESQL`. Achado de infraestrutura: `requirements-api.txt`/`deploy.yml` precisaram ganhar `qdrant-client`/`fastembed`/`GCP_PROJECT_ID`/`QDRANT_URL`/`QDRANT_API_KEY` — a imagem/serviço da API nunca tinham precisado falar com Vertex AI nem Qdrant antes desta feature |
| `CLOUD_COMPOSER_PROVISIONAMENTO` (`scripts/verificar_composer_producao.py`, `.github/workflows/verificar_composer_producao.yml`) | ✅ Shipado, **bloqueado e documentado** (`.claude/sdd/archive/CLOUD_COMPOSER_PROVISIONAMENTO/`) — ciclo efêmero real (Terraform provisiona → verifica → destrói), 8 bugs reais corrigidos, bloqueio final de instabilidade do ambiente não atribuível a bug de código, ambiente destruído e custo zerado | Primeira feature do projeto com um recurso GCP deliberadamente TEMPORÁRIO — Composer 3 custa ~US$300-400/mês rodando continuamente, desproporcional a uma DAG de 2 tasks/semana (achado do `/brainstorm`); decisão explícita do usuário de provisionar, verificar e destruir no mesmo ciclo, ao invés de manter permanente como todo recurso GCP anterior. 8 bugs reais encontrados e corrigidos: 2 de IAM (SA de Terraform sem `composer.environments.create`; faltava `actAs` sobre a SA de runtime do ambiente), `dags trigger` com `DagNotFound` por lag do metadatastore, `list_dag_runs` (Airflow 1.x) vs `list-runs` (Airflow 2.x, sintaxe certa do `composer-3-airflow-2`) com erro antes engolido por `2>/dev/null`, pacote `ingestion/` e seus pips nunca enviados ao ambiente na 1ª execução real, `storage dags import --source=DIR` não copia recursivamente (corrigido enviando arquivo por arquivo), scheduler e workers sincronizam a pasta GCS de forma independente (exigiu `sleep` de buffer), e interpolação de `${{ inputs.filtro }}` quebrando o bash quando o filtro tinha aspas internas (corrigido passando por `env:` em `diagnostico_cloud_run_logs.yml`, generalizado nesta feature para qualquer `resource.type` do Cloud Logging). **Achado final, não corrigido por decisão de custo**: 3 tentativas automáticas da DAG travaram no mesmo ponto (2 de 6 arquivos do modelo `multilingual-e5-large` baixados via `fastembed`, sem erro logado — suspeita de OOM no worker `SMALL`); redimensionar para `MEDIUM` (`workloads_config` explícito) não resolveu — o webserver do Airflow entrou em crash-loop sustentado por mais de 10h (`Worker was sent SIGKILL! Perhaps out of memory?`), nunca redescobrindo a DAG. Mesma disciplina de `LLM_REAL_VERTEX_AI` (quota do Vertex AI): documentar o bloqueio como achado real de infraestrutura, não como bug do projeto. O que FOI provado: o ambiente provisiona, a DAG é descoberta pelo scheduler, e as tasks executam código real (import do pacote `ingestion/` bem-sucedido, conexão real ao HuggingFace Hub iniciada) — só a conclusão com `success` (e a prova de idempotência que dependia dela) ficou bloqueada. Ambiente destruído via `terraform apply` (5 recursos removidos), custo zerado — 7ª posição do roadmap |
| `BIGQUERY_DATA_WAREHOUSE` (`scripts/sincronizar_bigquery.py`, `.github/workflows/sincronizar_bigquery.yml`) | ✅ Shipado (`.claude/sdd/archive/BIGQUERY_DATA_WAREHOUSE/`) — verificado com 2 execuções reais (82 linhas na carga inicial, zero duplicatas no rerun); 1 achado real de IAM corrigido | Espelha `pareceres_audit_log` (Cloud SQL) num dataset BigQuery PERMANENTE (`taxreformai_analytics.pareceres_historico`) — diferente do Composer (feature anterior, ciclo efêmero por custo), BigQuery tem perfil serverless, seguro para manter sempre provisionado. Motivada por necessidade real confirmada com o usuário (dashboard interno, relatório de cliente, análise de negócio). Sync incremental respeita o RLS já auditado do projeto sem nenhuma policy nova: itera cada tenant via `sessao_do_tenant()` (nenhum papel do Cloud SQL tem `BYPASSRLS`), com idempotência garantida por staging+`MERGE` pela chave `id` — não só pela janela do watermark, elimina o risco teórico de duplicata em empate exato de `created_at`. SA dedicada mínima (`taxreformai-bigquery-sync`) NÃO reaproveita `GCP_SA_KEY`: primeira feature em que uma credencial ADMIN do Postgres é usada por um job automático sem clique humano. É a primeira feature do projeto com `schedule: cron` — todo outro workflow de infra real é `workflow_dispatch`-only — decisão explícita do usuário, mitigada pelo escopo mínimo da SA. Achado real do `/build`: mesma classe de bug já vista no Composer — a SA de Terraform não tinha `bigquery.datasets.create`, corrigido reintroduzindo o mecanismo de extração de `terraform_sa_email` (removido ao destruir o Composer, reintroduzido para este novo uso legítimo). `AT-006` (cron disparando sozinho) fica pendente da passagem real do horário agendado — 10ª posição do roadmap |

Os 5 componentes centrais do blueprint (ingestão, motor, orquestração, API, frontend) existem agora, mais a segunda fonte de ingestão (TCU), a camada ETL real (Airflow, escrita), o CD para Cloud Run, o schema PostgreSQL real e conectado, o IPI consumindo a TIPI já ingerida, os 14 Anexos de redução (4 a zero por NCM, 6 a 60% por NCM, 4 a 60% por NBS) zerando/reduzindo CBS/IBS por item, o piso do Anexo XVI (art. 371, 2029-2077), a base de incidência do Imposto Seletivo (Anexo XVII), a integração de CBS/IBS ao Simples Nacional (Anexos XVIII-XXIII), o catálogo de SKUs com o primeiro CRUD de escrita multi-tenant do projeto, e agora a conexão real com Claude via Vertex AI substituindo 4 dos 5 nós fake da orquestração, mais um data warehouse BigQuery espelhando o audit log para consultas analíticas — 20 features shipadas (a 19ª, `CLOUD_COMPOSER_PROVISIONAMENTO`, shipada bloqueada e documentada — ver linha própria). A `DEPLOY_CLOUD_RUN` (2026-07-25, `.claude/sdd/archive/DEPLOY_CLOUD_RUN/`) tem **7/7 acceptance tests verificados contra infraestrutura real** — a primeira feature do projeto nesse patamar, seguida por `SCHEMA_POSTGRESQL`, `IPI_TIPI_MOTOR_CALCULO`, `REGRAS_TRIBUTARIAS_CACHE`, `ANEXOS_REDUCAO_ZERO_XII_XIII_XV`, `ANEXOS_REDUCAO_PERCENTUAL_NCM`, `ANEXOS_REDUCAO_PERCENTUAL_NBS`, `ANEXO_XVII_IMPOSTO_SELETIVO_INCIDENCIA`, `API_EMPRESA_SKUS` e `LLM_REAL_VERTEX_AI` (esta última com o caminho 200 completo ainda bloqueado por quota do Vertex AI no Console do GCP — ação do usuário, não bug). `ANEXO_XVI_PISO_ALIQUOTA_PROPRIA` e `SIMPLES_NACIONAL_CBS_IBS_TRANSICAO` são as únicas features do projeto sem NENHUMA superfície de infraestrutura (Python puro) — nada a verificar contra Cloud SQL. A aplicação está pública, funcionando, e agora persiste dados de verdade — e o endpoint conversacional (`POST /v1/tax/query`) finalmente chama um LLM real, não mais fake.

### Fontes legais ingeridas (verificado em produção, 2026-07-25)

Coleção Qdrant `legislacao_tributaria` com **6866 pontos**; verificação E2E da busca híbrida
**APROVADA** (Bloco A 5/5, Bloco B 2/2) — o critério pendente desde o ship de
`PIPELINE_INGESTAO_LEGAL` está fechado.

| Fonte | Documento | Artigos | Chunks |
|-------|-----------|---------|--------|
| Planalto (DOU) | LCP 214/2025 | 580 | 3417 |
| CGIBS | Resolução 6/2026 (regulamenta o IBS) | 617 | 3443 |
| RFB (SIJUT2) | Soluções de Consulta — ementas | 28 | 28 |
| TCU | Resolução 388/2026 | 16 | 27 |

Das 4 famílias mapeadas no blueprint (seção 4.1), **3 estão cobertas**: DOU (via Planalto),
Comitê Gestor do IBS e RFB. Mais o TCU, que não estava mapeado.

**SPED / IBPT — decisão de 2026-07-25: fora do pipeline de ingestão.** São dados **tabulares**
(NCM → alíquota aproximada, Lei 12.741/2012), não legislação: consulta é por chave exata, não
semântica, então embedá-los no Qdrant seria a ferramenta errada. Pertencem ao schema PostgreSQL
e são consultados deterministicamente — o padrão que `aliquotas_ipi_tipi` (migração 004) e
`anexos_reducao`/`anexos_reducao_catalogo` (migrações 005→007→008→009→010) já concretizaram. (A tabela genérica `regras_tributarias_cache`
que a seção 7 do blueprint previa para isso **foi removida** pela migração 006: sua forma —
alíquota absoluta, um NCM por linha, sem exceções — não serve para nenhum regime diferenciado real.)
Além disso a tabela IBPT exige **cadastro de empresa** e é produto licenciado de um instituto
privado — não legislação em domínio público como as outras três. É decisão de licenciamento,
não técnica. A tabela ainda tem vigência de ~2 meses e é reeditada bimestralmente, o que a torna
um problema de sincronização periódica, não de ingestão documental.

CI/CD: `ci.yml` roda pytest (com **container de serviço PostgreSQL real** — os testes de schema/RLS
executam contra Postgres de verdade, não SQLite) + testes de frontend a cada push/PR;
`terraform.yml` faz plan/apply via `workflow_dispatch`; `deploy.yml` publica no Cloud Run e liga a
API ao Cloud SQL via `workflow_dispatch`; `ingestao.yml` roda a ingestão real + verificação E2E da
busca híbrida via `workflow_dispatch` (guarda `INGERIR`, também usado por `verificar_lc227` para
diagnósticos de leitura pontuais contra o Qdrant); `migrar_banco.yml` aplica migrações e
prova RLS + a leitura de IPI e dos 10 Anexos de redução (4 a zero, 6 a 60%) pelo papel de
runtime contra o Cloud SQL real via `workflow_dispatch` (guarda `MIGRAR`, input `verificar_reducao`).
O bucket GCS e o Cloud SQL já foram aplicados de verdade; o state do Terraform é remoto
(`gs://taxreformai-dev-tfstate`).

Próximo ciclo: com a "segunda leva" (posições 12-17), `API_EMPRESA_SKUS`/`LLM_REAL_VERTEX_AI`
(posições 3-5), `REMOVER_FAKE_HISTORICO` (posição 6, resolvida como efeito colateral),
`CLOUD_COMPOSER_PROVISIONAMENTO` (posição 7), `DIAGNOSTICO_BUSCA_HIBRIDA` (posição 9, achada
já resolvida em 2026-08-04 sem ciclo dedicado — o root-cause do "miss" já tinha sido corrigido
em 2026-07-25, commit `89d44a1`, e reconfirmado por uma run real de `ingestao.yml` de
2026-07-28) e `BIGQUERY_DATA_WAREHOUSE` (posição 10) completas, seguem as posições 8 e 11
restantes da "primeira leva" (`VERIFICACAO_FRONTEND_NAVEGADOR` — bloqueada agora pela extensão
do Chrome desconectada de novo —, `FILA_ASSINCRONA_CELERY_REDIS`) —
`ROADMAP_SEQUENCIA_AUDITORIA_2026-07.md`. Verificação manual em navegador ainda pendente, agora
possível porque há URL pública (a extensão do Chrome não estava conectada nas últimas tentativas).
**SPED/IBPT** seguem fora de escopo — ver decisão abaixo.

**Achado do `/design` de `ANEXOS_REDUCAO_ZERO_XII_XIII_XV` (posição 12) resolvido pela posição 13:**
os arts. 144, **II** e 145, **II** da LCP 214/2025 (mais o art. 146, §2º, achado depois no `/define`
da própria posição 13) reduzem a **zero** os Anexos **IV**, **V** e **VI** quando o adquirente é
órgão público ou entidade CEBAS credenciada ao SUS. `ANEXOS_REDUCAO_PERCENTUAL_NCM` resolveu isso
com um campo novo de payload, `comprador_tipo` — não é mais uma pendência.

## Stack planejada (extraída de `contexto.md`)

- **Frontend:** Next.js 14 (App Router) + TailwindCSS + Shadcn UI + TanStack Query
- **Backend/API:** FastAPI (Python 3.11+) + Pydantic v2 + Celery/Redis (fila assíncrona)
- **Orquestração multi-agente:** LangGraph / CrewAI + Anthropic Claude via Vertex AI (Claude 3.5 Sonnet e Haiku)
- **Motor determinístico:** Python puro (sandbox), sem LLM, para cálculos de IVA Dual/Split Payment
- **Banco relacional:** Cloud SQL (PostgreSQL 16) — **real, aplicado e conectado** desde `SCHEMA_POSTGRESQL` (2026-07-27)
- **Banco vetorial:** Qdrant Cloud (busca híbrida densa/esparsa, embeddings `intfloat/multilingual-e5-large` + BM25 esparso — o blueprint pede BGE-M3, mas o `fastembed` não o suporta; ver `ingestion/embedding/hybrid_embedder.py`)
- **Data lake:** Google Cloud Storage (GCS)
- **Ingestão/ETL:** Scrapy / Playwright orquestrados via Airflow (Cloud Composer)
- **Infraestrutura:** GCP — Cloud Run (serverless), Cloud Composer, BigQuery, região `southamerica-east1`

## Estrutura

```
TaxReformAI/
├── contexto.md              # Blueprint completo: produto, arquitetura, infra, DB, API, pricing
├── CLAUDE.md                 # Este arquivo
├── ingestion/                # PIPELINE_INGESTAO_LEGAL + INGESTAO_TCU — scraper, parser AST, chunker, embedder, indexador Qdrant
│   ├── scraper/               # PlanaltoScraper + TCUScraper (LegalSource)
│   ├── parser/                 # AST hierárquica (Secao recursiva + Artigo/Parágrafo/Inciso/Alínea) + resolucao_parser.py (PDF/TCU)
│   ├── chunking/                # Chunk (Pydantic) + chunker parent-child
│   ├── embedding/                 # Hybrid embedder (multilingual-e5-large + BM25)
│   ├── storage/                    # RawStorage (GCS real + fake para testes)
│   ├── indexing/                    # QdrantIndexer
│   └── pipeline.py                   # CLI de orquestração (executar_pipeline, parser injetável)
├── dags/                     # INGESTAO_TCU_E_ETL_AIRFLOW — DAG real do Airflow/Cloud Composer
│   └── ingestao_legal_dag.py  # TaskFlow API; não importável neste sandbox (apache-airflow ausente), só revisão de código
├── motor_calculo/            # MOTOR_DETERMINISTICO_CALCULO + SCHEMA_POSTGRESQL — cálculo tributário
│   ├── fases.py               # FaseTransicao + fase_para(ano)
│   ├── regras_fiscais.py       # RegraFiscal (alíquota `Decimal | None` + fonte por tributo) + AliquotaNaoDisponivelError
│   ├── tabela_aliquotas.py      # TabelaAliquotas (Protocol) + seed com artigos reais (2026 completo, 2027-28 parcial)
│   ├── engine.py                 # TaxCalculatorEngine — CBS/IBS/IS (reforma)
│   ├── reducoes.py                 # aplicar_reducao_a_zero — override por ITEM depois de calcular(); CBS/IBS a zero, IS intacto, líquido recomposto. Python puro, zero infra
│   ├── regime_atual.py            # PIS/COFINS + ICMS interestadual + ICMS interno (27 UFs) + ISS piso/teto (regime VIGENTE) — cada alíquota citada por artigo real; IPI NÃO mora aqui (precisa de banco), vive em api/ipi.py
│   ├── piso_aliquota_ibs.py        # ANEXO_XVI_PISO_ALIQUOTA_PROPRIA — piso_aliquota_ibs(ano), tabela de 49 anos (2029-2077, art. 371); Python puro, ZERO infraestrutura
│   └── simples_nacional.py          # SIMPLES_NACIONAL_CBS_IBS_TRANSICAO — calcular_simples_nacional(atividade, RBT12, receita_mes, ano); 6 Anexos (XVIII-XXIII), fórmula do art. 18 da LC 123/2006, teto de ISS, MEI; Python puro, ZERO infraestrutura
├── orquestracao/              # ORQUESTRACAO_MULTIAGENTE + LLM_REAL_VERTEX_AI — 5 nós, executor sequencial
│   ├── estado.py                # State (Pydantic) + TransicaoAuditavel + registrar_transicao
│   ├── executor.py                # executar_consulta(state, deps) — encadeia os 5 nós, sem depender de langgraph
│   ├── grafo.py                     # construir_grafo(deps) — versão LangGraph via functools.partial; não exercitado neste sandbox
│   ├── config.py                     # OrquestracaoSettings.from_env() — GCP_PROJECT_ID/VERTEX_AI_REGION/QDRANT_*
│   ├── dependencias.py                 # DependenciasOrquestracao (cliente_llm+qdrant_indexer+embedder) + criar_dependencias_reais/fake + FakeEmbedder/FakeQdrantSearcher
│   ├── llm/cliente.py                    # ClienteLLM (Protocol) + ClienteVertexAI (AnthropicVertex, region=global) + ClienteLLMFake + LLMIndisponivelError
│   └── nos/                               # classificador (Haiku, mascara PII antes de tudo) → pesquisador_legal (busca híbrida real) → extrator_regras (Sonnet, campos estruturados sempre vencem) → deterministico (motor_calculo, sem LLM) → sintetizador (Sonnet, guardrail rejeita parecer que não reproduz TODOS os valores calculados)
├── db/                        # SCHEMA_POSTGRESQL — schema real no Cloud SQL (taxreformai-pg)
│   ├── migrations/              # 001 (tabelas) → 002 (RLS) → 003 (privilégio mínimo do papel app) → 004 (aliquotas_ipi_tipi + GRANT) → 005 (Cesta Básica/Anexo I) → 006 (DROP guardado de regras_tributarias_cache) → 007 (RENAME para anexos_reducao_zero* + chave (anexo,item,sub_item) + prefixo de 2 dígitos) → 008 (seed dos Anexos XII/XIII/XV) → 009 (catálogo `anexos_reducao_catalogo`; RENAME final `anexos_reducao*`, sem o `_zero`) → 010 (seed dos Anexos IV/V/VI/VII/VIII/IX: 261 itens/392 prefixos + 7 asserções, incluindo a prova SQL da remissão do Anexo VII) → 011 (catálogo +4 Anexos NBS; tabelas NOVAS `anexos_reducao_nbs`/`anexos_reducao_nbs_prefixo`, dedicadas, nunca comingladas com as NCM; seed de II/III/XI) → 012 (seed do Anexo X, 47 itens, mapeamento item-a-inciso do art. 139 documentado — fechado no mesmo dia via PDF da Câmara dos Deputados) → 013 (`imposto_seletivo_incidencia`/`_ncm`, 6 categorias/24 prefixos do Anexo XVII, com asserção própria provando que as categorias não se sobrepõem) → 014 (`empresa_skus` ganha `natureza` + `CHECK` de exclusividade NCM/NBS; `ncm_code` vira NULLABLE — primeira migração que ALTERA uma tabela de escrita multi-tenant, não uma tabela de referência pública)
│   ├── migrador.py               # Runner idempotente, sem ORM
│   └── repositorio.py             # sessao_do_tenant, registrar_parecer, resolver_tenant, buscar_ipi_por_ncm, buscar_reducao_por_prefixo (ambos em lote, 1 query; a segunda já traz o percentual/ordinal/condição de comprador do catálogo via JOIN), e as 7 funções de `empresa_skus` (criar/listar/buscar/atualizar/excluir/upsert/lookup em lote — todas via `sessao_do_tenant`, primeira ESCRITA multi-tenant do projeto)
├── api/db.py                # Pool de conexão (Depends, overridável em teste — mesmo padrão de api/config.get_settings)
├── api/audit.py              # registrar_com_seguranca — audit log que NUNCA propaga exceção
├── api/ipi.py                # IPI_TIPI — normalizar_ncm + resolver_item (5 situações) + consultar_ipi_com_seguranca; gêmeo de LEITURA do audit.py, também nunca propaga
├── api/ncm.py                # Vocabulário da NCM/SH — digitos_ncm (8 dígitos canônicos) + prefixos_ncm ({2,4,5,6,7,8}, sem o 3, que a NCM não tem); UMA noção de "NCM válido" para IPI e redução
├── api/reducao.py            # ANEXOS_REDUCAO (10 Anexos NCM: 4 a zero + 6 a 60%) — resolver_item (6 situações, + comprador_tipo) + consultar_com_seguranca + formatar_item; desempate de 6 componentes; degradação CONSERVADORA (alíquota geral, nunca null)
├── api/nbs.py                # ANEXOS_REDUCAO_PERCENTUAL_NBS — digitos_nbs (9 dígitos, classificador de topo "1") + prefixos_nbs ({5,6,7,9}); vocabulário PRÓPRIO, nunca comingle com NCM
├── api/reducao_nbs.py        # ANEXOS_REDUCAO_PERCENTUAL_NBS — resolver_item_nbs (Anexos II/III/X/XI completos); SituacaoReducaoNbs com CONDICAO_NAO_SATISFEITA (gating, inverso do upgrade zero-por-comprador do lado NCM); cobre tanto a condição de comprador/vendedor (XI) quanto a de nacionalidade de conteúdo (X)
├── api/imposto_seletivo.py   # ANEXO_XVII_IMPOSTO_SELETIVO_INCIDENCIA — resolver_item (6 situações, incl. CONDICAO_NAO_SATISFEITA reaproveitada de reducao_nbs.py); SEM desempate cross-categoria (faixas de NCM comprovadamente disjuntas na própria migração); NUNCA calcula valor/percentual de IS, por desenho
├── api/schemas_simples_nacional.py  # SIMPLES_NACIONAL_CBS_IBS_TRANSICAO — PayloadSimplesNacional/RespostaSimplesNacional, payload DEDICADO sem lista de itens (receita do mês é campo escalar); consumido por POST /v1/tax/simulate-simples-nacional em api/routers/simulate.py, endpoint independente de engine.py/TabelaAliquotasSeed
├── api/empresa_skus.py       # API_EMPRESA_SKUS — resolver_ncm_nbs_do_item (4 situações, 422 só para MERCADORIA — SERVICO sem nbs já era válido antes desta feature), validar_exclusividade (reaproveitada pelo schema Pydantic E pelo parser de CSV), parsear_linha_csv
├── api/schemas_empresa_skus.py  # API_EMPRESA_SKUS — Payload/RespostaSku; field_validator normaliza ncm_code/nbs_code (achado do security-reviewer: CRUD individual não validava formato, só o upload CSV)
├── api/routers/empresa_skus.py  # API_EMPRESA_SKUS — 6 endpoints /v1/tax/skus (CRUD completo + upload CSV); db_pool is None -> 503 (único endpoint do projeto onde infra ausente é ERRO, não degradação — não existe "criar SKU sem banco")
├── api/dependencias_orquestracao.py  # LLM_REAL_VERTEX_AI — get_dependencias_orquestracao(), provider FastAPI Depends cacheado (mesmo padrão de api/db.get_db_pool), consumido por api/routers/query.py
├── api/Dockerfile           # DEPLOY_CLOUD_RUN — build context é a RAIZ do repo; copia api/, motor_calculo/, orquestracao/, ingestion/ E db/
├── frontend/Dockerfile       # DEPLOY_CLOUD_RUN — multi-stage (deps→builder→runner), Next.js standalone, usuário não-root
├── requirements-api.txt      # Deps de runtime da API (fastapi/uvicorn/pydantic/psycopg/anthropic[vertex]/qdrant-client/fastembed desde LLM_REAL_VERTEX_AI) — o que vai para a imagem; requirements.txt inclui via `-r`
├── infra/terraform/         # Bucket GCS + Artifact Registry + SA de deploy + Cloud SQL (taxreformai-pg) + Vertex AI (LLM_REAL_VERTEX_AI: aiplatform.googleapis.com + roles/aiplatform.user para taxreformai-runtime) — state remoto em gs://taxreformai-dev-tfstate
├── .github/workflows/        # ci.yml (pytest + frontend, com Postgres real de serviço) + terraform.yml + deploy.yml (Cloud Run + Cloud SQL + 1 chamada real a /v1/tax/query desde LLM_REAL_VERTEX_AI) + ingestao.yml + migrar_banco.yml — os 4 últimos só por workflow_dispatch
├── scripts/                  # verificar_busca_hibrida.py, aplicar_migracoes.py, popular_tenants.py, verificar_rls_producao.py, verificar_audit_log_gravado.py, ingerir_tipi.py, verificar_ipi_producao.py, verificar_reducao_producao.py, verificar_empresa_skus_producao.py (primeiro com ESCRITA, prova isolamento entre 2 tenants) — todos rodam só via workflow, nunca local
├── tests/                    # 614 testes (pytest) — os de schema/RLS/TIPI/Anexos/SKUs pulam sem DATABASE_URL, os de ClienteVertexAI pulam sem anthropic[vertex] instalado, rodam de verdade no CI
├── .env.example              # Template de variáveis — .env local fica só com config não-sensível (ex: FRONTEND_ORIGINS); credenciais reais vivem em GitHub Secrets
└── .claude/sdd/               # Documentos do workflow SDD (features/, reports/, archive/)
```

## Arquivos-chave

| Arquivo | Função |
|---------|--------|
| `contexto.md` | Fonte única de verdade do projeto: visão de produto, ICP/personas, linha do tempo da reforma tributária, arquitetura multi-agente (5 agentes especialistas + matriz de modelos Claude), pipeline ETL de dados públicos, chunking hierárquico AST, stack GCP, motor de cálculo (exemplo), modelagem PostgreSQL, spec da API `/v1/tax/simulate`, modelo de negócio SaaS |
| `ingestion/pipeline.py` | Ponto de entrada do pipeline de ingestão — `executar_pipeline()` é testável com fakes (`parser` injetável, reaproveitado pelo TCU); execução real só via `dags/ingestao_legal_dag.py` num Cloud Composer real, nunca local |
| `motor_calculo/engine.py` | Ponto de entrada do motor de cálculo — `TaxCalculatorEngine(tabela).calcular(...)`, sem dependências externas |
| `.claude/sdd/archive/MOTOR_DETERMINISTICO_CALCULO/SHIPPED_2026-07-23.md` | Lições aprendidas da feature já shipada — vale ler antes de iniciar a próxima |
| `.claude/sdd/archive/PIPELINE_INGESTAO_LEGAL/SHIPPED_2026-07-24.md` | Lições da primeira feature de ingestão — padrão `Protocol` real/fake (`RawStorage`/`LegalSource`) que se repetiu em todas as features seguintes |
| `.claude/sdd/archive/INGESTAO_TCU_E_ETL_AIRFLOW/SHIPPED_2026-07-24.md` | Lições aprendidas da feature TCU/Airflow — bugs reais de parsing de PDF (referência cruzada, bloco de assinatura) só apareceram contra o documento real |
| `dags/ingestao_legal_dag.py` | DAG real do Airflow/Cloud Composer que substitui `pipeline.py` como orquestrador — não executável neste sandbox, só revisão de código |
| `.claude/sdd/archive/SCHEMA_POSTGRESQL/SHIPPED_2026-07-27.md` | Lições da feature do schema — a mais cara: nenhum papel no Cloud SQL (nem `postgres`) é superusuário de verdade, diferente de Postgres autogerido; qualquer config de papel testada só contra Postgres genérico merece diagnóstico direto contra o serviço gerenciado antes de assumir paridade |
| `motor_calculo/regime_atual.py` | PIS/COFINS + ICMS interestadual — ponto de entrada do regime vigente, `TabelaPisCofins().buscar(regime)` e `icms_interestadual(uf_origem, uf_destino, bem_importado=...)` |
| `motor_calculo/piso_aliquota_ibs.py` | Ponto de entrada do piso do art. 371/Anexo XVI — `piso_aliquota_ibs(ano)`, `None` fora de [2029, 2077]; NUNCA calcula alíquota absoluta, por desenho (o multiplicador — alíquota de referência por esfera federativa — é grandeza calculada de execução fiscal real, fora de escopo permanente). Consumido por `GET /v1/tax/piso-aliquota-ibs/{ano}` (o único caminho que alcança o dado hoje — `/v1/tax/simulate` já 422 para todo `ano_operacao >= 2027`) e pelo campo forward-compatible `RespostaSimulacao.piso_aliquota_ibs` |
| `db/repositorio.py` | Ponto de entrada do acesso a dados — `sessao_do_tenant()` é o único jeito correto de tocar tabelas com RLS |
| `db/migrations/005_cesta_basica_anexo_i.sql` | A transcrição literal do Anexo I (26 itens, 95 prefixos) com a URL da fonte primária no cabeçalho — é o documento de auditoria da Cesta Básica, não só um script. A CHECK `prefixo = regexp_replace(texto_ncm, '[^0-9]', '', 'g')` impede transcrição inconsistente. **Não se edita**: migração aplicada é histórico, e a forma dela foi generalizada pelas 007/009 |
| `db/migrations/010_anexos_reducao_percentual_ncm.sql` | A transcrição literal dos Anexos IV/V/VI/VII/VIII/IX (261 itens, 392 prefixos), com URL da fonte e 7 asserções — inclusive uma que faz rollback se o desempate por especificidade deixar de honrar a remissão que o próprio Anexo VII escreve para os Anexos I/XV (itens 4, 5, 6, 14 e 15) |
| `api/reducao.py` | Ponto de entrada dos 10 Anexos de redução por NCM (4 a zero, 6 a 60%) — `resolver_item(natureza, ncm, consulta, comprador_tipo)`, função pura com 6 situações; `EXCLUIDA_EXPRESSAMENTE` (o Anexo exclui o código) NÃO é `FORA_DO_ANEXO`. O desempate de 6 componentes é `(len(prefixo), percentual_efetivo_com_comprador, percentual_reducao_do_catálogo, -anexo_ordem, -item, -sub_item)` — o 2º componente resolve os 35 pares que citam o MESMO NCM de 8 dígitos em dois Anexos diferentes |
| `api/reducao_nbs.py` | Ponto de entrada dos 4 Anexos por NBS (II/III/X/XI, todos completos) — `resolver_item_nbs(natureza, nbs, consulta, comprador_tipo, conteudo_nacional_majoritario, vendedor_capital_brasileiro_qualificado)`, função pura com uma situação SEM equivalente NCM: `CONDICAO_NAO_SATISFEITA` (nem o Anexo X nem o XI têm "60% incondicional" — a condição HABILITA os 60% a partir da alíquota geral, não os REDUZ a partir de 60%, inverso de IV/V/VI) |
| `db/migrations/011_anexos_reducao_percentual_nbs.sql` | Transcrição literal dos Anexos II (9 itens), III (30 itens, incluindo a anomalia de 1 dígito do item 29, completada com nota) e XI Bloco 1 (5 de 14 sub-itens resolvíveis); 4 linhas novas em `anexos_reducao_catalogo` (incl. o Anexo X, semeado só na migração 012) |
| `db/migrations/012_anexo_x_producoes_nacionais.sql` | Transcrição literal dos 47 itens NBS do Anexo X + o texto integral do art. 139 (obtido via PDF da Câmara dos Deputados, não pela mesma rota da 011); documenta o mapeamento item-a-inciso como leitura de boa-fé, não citação literal — 45 dos 47 itens exigem `conteudo_nacional_majoritario` (só os itens 22 e 40, incisos V/VI, ficam de fora) |

## Convenções

- **Linter:** `ruff` (`ruff check .` — configurado e limpo; `pyproject.toml` declara `select` explícito desde 2026-07-25, para não depender do default de cada versão instalada)
- **Formatter:** não configurado explicitamente (código segue `ruff format` implicitamente)
- **Testes:** `pytest` — `python3 -m pytest tests/ -v` (436 testes: 433 passed + 3 skipped local; os de `db/` pulam sem `DATABASE_URL`, rodam de verdade no CI contra um container `postgres:16`)

## Como rodar

```bash
pip install -r requirements.txt   # set completo (dev/CI); qdrant-client, google-cloud-storage, fastembed, typer, apache-airflow, anthropic[vertex] não instaláveis via pip normal neste sandbox (PEP 668) — instaláveis via `pip install --target=`, ver BUILD_REPORTs
pip install -r requirements-api.txt # só o runtime da API (fastapi/uvicorn/pydantic/psycopg) — é o que a imagem do Cloud Run instala
python3 -m pytest tests/ -v        # roda os testes sem precisar de nenhuma credencial (usa fakes, e pula os de schema sem DATABASE_URL); exige poppler-utils (pdftotext) no sistema para os testes do TCUScraper
```

**Política do projeto: infraestrutura real nunca roda local.** `.env` local é só para o que os testes fake precisam (hoje, nada de credencial real) — nunca para disparar a pipeline de ingestão contra GCS/Qdrant reais na sua máquina. Credenciais reais (`GCP_PROJECT_ID`, `GCS_BUCKET_NAME`, `GCP_SA_KEY`, `QDRANT_URL`, `QDRANT_API_KEY`) vivem em **GitHub Secrets** do repositório e são consumidas por workflows (`terraform.yml` já aplicou o bucket GCS real; a execução da pipeline de ingestão em si ainda depende de `dags/ingestao_legal_dag.py` rodar num Cloud Composer real, ou de um workflow `workflow_dispatch` equivalente). O motor de cálculo (`motor_calculo/`) não precisa de nenhuma infraestrutura — roda direto, local ou não.

## Deploy (Cloud Run)

`.github/workflows/deploy.yml`, **só por `workflow_dispatch`** (nunca no push) com `target` = `api` | `frontend` | `both` e `confirm` = `DEPLOY`. Publica imagens no Artifact Registry tagueadas pelo SHA do commit e atualiza dois serviços em `southamerica-east1`: `taxreformai-api` e `taxreformai-frontend`.

**Ordem importa, por uma dependência circular real:** `NEXT_PUBLIC_API_BASE_URL` é embutida no bundle JS em *build time*, enquanto `FRONTEND_ORIGINS` é lida pela API em *runtime*. O workflow resolve isso lendo as URLs existentes primeiro, deployando a API, buildando o frontend com a URL real da API, e por fim reconciliando o `FRONTEND_ORIGINS` da API (só cria revisão nova se o valor mudou).

**O smoke test reprova o job** — e isso não é redundante: os defaults de `api/main.py` (`FRONTEND_ORIGINS=localhost:3000`) e `api/config.py` (`API_KEYS={}`) fazem um deploy incompleto subir um serviço que responde **200 em `/health`** e falha 100% das requisições reais. Por isso o smoke test também faz um `POST /v1/tax/simulate` com chave real e confere o header `access-control-allow-origin`.

Secrets necessários: `GCP_PROJECT_ID`, `GCP_DEPLOYER_SA_KEY` (SA `taxreformai-deployer`, escopada — não reusar `GCP_SA_KEY`, que é do Terraform) e `API_KEYS` (JSON `{"chave":"tenant_id"}`). O `tenant_id` do corpo de `POST /v1/tax/simulate` precisa bater com o da credencial, senão a resposta é **403**.

## Estado do runbook

**A aplicação está no ar e persiste dados de verdade.** Serviços públicos:

| Serviço | URL |
|---------|-----|
| API | `https://taxreformai-api-as2g43xasa-rj.a.run.app` |
| Frontend | `https://taxreformai-frontend-as2g43xasa-rj.a.run.app` |

Identidade de runtime: `taxreformai-runtime@taxreformai-dev.iam.gserviceaccount.com`, originalmente
criada **sem role de projeto** — só `roles/cloudsql.client` e leitura do próprio secret de senha,
o mínimo para falar com o Cloud SQL. `LLM_REAL_VERTEX_AI` concedeu a essa SA a primeira role de
projeto (`roles/aiplatform.user`, para chamar Claude via Vertex AI) — desvio intencional e
documentado desse princípio original, não um descuido: `roles/aiplatform.user` não tem
equivalente restrito a um recurso específico para modelos de publisher do Model Garden.

## Banco de dados (Cloud SQL)

Instância `taxreformai-pg` (PostgreSQL 16, `db-f1-micro`, `southamerica-east1`), schema aplicado e
API conectada — ver `.claude/sdd/archive/SCHEMA_POSTGRESQL/`.

- **Dois papéis**: `taxreformai_admin` (migrações — `migrar_banco.yml`, guarda `MIGRAR`) e
  `taxreformai_app` (runtime da API, privilégio mínimo — `db/migrations/003_papel_da_aplicacao.sql`).
  Senhas geradas pelo Terraform, vivem no Secret Manager (`taxreformai-pg-admin-password`,
  `taxreformai-pg-app-password`), nunca em texto plano em workflow.
- **RLS isola tenants de verdade**, provado contra o Cloud SQL real (não só inferido) por
  `scripts/verificar_rls_producao.py`, rodável via `migrar_banco.yml` (`verificar_rls=sim`).
- **Achado que vale lembrar**: nenhum papel no Cloud SQL — nem `postgres` — tem `rolsuper=true`.
  A migração 003 tentou originalmente revogar `SUPERUSER`/`BYPASSRLS`, o que é impossível de
  executar lá (só quem já é superusuário pode tocar esse atributo) e desnecessário (a plataforma
  nunca concede o bit). Ver a Lição 3 do SHIPPED da feature antes de portar qualquer config de
  papel testada só contra Postgres genérico.
- **Audit log gravando em produção**, confirmado por consulta separada depois do smoke test do
  deploy (`deploy.yml`, passo "Verificar que o audit log foi gravado de verdade").
- **`aliquotas_ipi_tipi`** (migração 004, 9231 NCMs) é lida em runtime pelo `/v1/tax/simulate`.
  O `GRANT SELECT` ao papel `taxreformai_app` **nunca havia sido exercitado por nenhum SELECT** —
  e, como a falha degrada silenciosamente (200 com `total_ipi: null`), só um teste ativo a
  detecta: `scripts/verificar_ipi_producao.py`, via `migrar_banco.yml` (`verificar_ipi=sim`),
  conecta com o papel de RUNTIME e falha ruidosamente. O smoke test do `deploy.yml` fecha o E2E
  exigindo `regime_vigente.total_ipi` não-nulo.
- **`anexos_reducao` / `anexos_reducao_ncm` / `anexos_reducao_catalogo`** (migrações 005 → 007 → 008
  → 009 → 010; 321 itens + 540 prefixos) guardam os **10 Anexos de redução** da LCP 214/2025: 4 a
  ZERO — I (art. 125), XII (art. 144), XIII (art. 145), XV (art. 148) — e 6 a **60%** — IV (art. 131),
  V (art. 132), VI (art. 133, §1º), VII (art. 135), VIII (art. 136), IX (art. 138). Nasceram como
  `cesta_basica_anexo_i*`, viraram `anexos_reducao_zero*` (007) e foram generalizadas de novo pela
  009 — o catálogo (`anexos_reducao_catalogo`, 10 linhas) existe porque **117 pares de NCM se
  sobrepõem** entre os dois grupos (35 no MESMO código de 8 dígitos), então zero e 60% não podem
  viver em tabelas separadas: precisam de uma ordem total só, com um desempate de 6 componentes.
  Mesmo modo de falha silencioso do IPI, e **pior**: um `GRANT` faltando não gera erro nem campo
  nulo — gera a **alíquota geral da fase**, que é exatamente a resposta de antes da feature. Ela
  "funciona" verde sem fazer nada. Só um teste ativo detecta: `scripts/verificar_reducao_producao.py`
  (`migrar_banco.yml`, `verificar_reducao=sim`, 14 casos, incluindo a precedência normativa Anexo
  I/XV sobre o VII e a condição de comprador dos Anexos IV/V/VI) mais 3 chamadas do smoke test do
  `deploy.yml` — deliberadamente separadas da do IPI, porque juntar os payloads faria `total_ipi`
  depender de NCMs específicos estarem na TIPI ingerida.
- **A migração 007 (e depois a 009) renomeiam tabelas em uso**, então a ordem é `migrar_banco.yml` →
  `deploy.yml`, nessa sequência. A janela entre as duas é declarada e aceita: a API antiga consulta
  um nome que não existe mais, cai no `except` de `consultar_com_seguranca` e devolve 200 com a
  alíquota geral. Nenhum 5xx, nenhum cálculo errado para menos.
- **`regras_tributarias_cache` não existe mais** (migração 006, `DROP` guardado por uma checagem
  de "está vazia?"). Era código morto de schema desde a 001.
- **`comprador_tipo`** (`ORGAO_PUBLICO` / `ENTIDADE_CEBAS_SUS`, opcional) no payload de
  `/v1/tax/simulate` — os Anexos IV, V e VI (arts. 144-II, 145-II, 146-§2º) são reduzidos a zero
  quando o comprador é órgão público ou entidade CEBAS credenciada ao SUS, 60% caso contrário. É
  DECLARATÓRIO: a simulação não verifica imunidade nem consulta o SUS, só escolhe entre dois
  `Decimal` já carregados no catálogo.
- **`anexos_reducao_nbs` / `anexos_reducao_nbs_prefixo`** (migrações 011 + 012; 91 itens/90
  prefixos — Anexos II, III, X e XI, **todos completos**) guardam o **primeiro vocabulário do
  projeto por NBS** (Nomenclatura Brasileira de Serviços, 9 dígitos, classificador de topo sempre
  "1") — tabelas DEDICADAS, nunca comingladas com `anexos_reducao`/`anexos_reducao_ncm`: um prefixo
  NBS truncado de 5 dígitos tem o MESMO comprimento que um prefixo NCM válido de 5 dígitos, e só
  tabelas/consultas separadas tornam a colisão estruturalmente impossível. `anexos_reducao_catalogo`
  tem 14 linhas. O Anexo XI introduz uma polaridade de condição INVERSA à de IV/V/VI: lá, 60% é o
  padrão e o comprador qualificado ZERA a alíquota; aqui, a alíquota GERAL é o padrão e o comprador
  (art. 142, I) OU o vendedor com sócio brasileiro ≥20% do capital (art. 142, II, só para serviços
  de segurança da informação/cibernética) é que HABILITA os 60% — daí a situação nova
  `CONDICAO_NAO_SATISFEITA` em `api/reducao_nbs.py`, sem equivalente no lado NCM. O Anexo X (art.
  139, produções artísticas/culturais/audiovisuais) usa o MESMO mecanismo de gating para a condição
  de NACIONALIDADE DE CONTEÚDO (art. 139, §1º): 45 dos 47 itens NBS exigem
  `conteudo_nacional_majoritario=True`, só os itens 22 (feiras de negócios) e 40 (museus) ficam de
  fora (incisos V/VI, fora do §1º).
- **O mapeamento dos 47 itens do Anexo X aos incisos (I-VIII) do art. 139 (migração 012) é uma
  leitura de boa-fé, não uma citação jurisprudencial** — o próprio texto do Anexo X não tem coluna
  de inciso, então cada item precisou ser lido contra a lista de categorias do caput. A migração
  documenta 3 grupos por certeza decrescente: itens 1-21/46-48 (licenciamento de direitos autorais)
  mapeados ao inciso VII por proximidade textual (nenhum inciso cita "obras literárias" ou
  "fonogramas" literalmente); itens 23-35/41 (suporte à produção audiovisual) cuja PRÓPRIA descrição
  já diz "produções nacionais"; e itens 55-57 (sonorização/palco) que servem QUALQUER produção do
  art. 139 — tratados conservadoramente como exigindo a condição, mesmo que a lei os dispense quando
  servem eventos dos incisos IV/V/VI, porque o código NBS não distingue qual produção está sendo
  atendida. Ver `db/migrations/012_anexo_x_producoes_nacionais.sql` para o texto integral do art.
  139 e o raciocínio completo.
- **O Anexo X não pôde ser semeado no `/build` original (2026-07-31) pela mesma razão já registrada
  para `planalto.gov.br`/`nbs.economia.gov.br`**: o corpo de 544 artigos da LCP 214/2025 excedia o
  que a ferramenta de leitura web processava de uma vez. Fechado no MESMO DIA, como continuação
  pós-ship, ao baixar o "Texto Atualizado" (PDF, 298 páginas) do mirror oficial da Câmara dos
  Deputados (LegIn) e extrair o texto com `pdftotext -layout` — mesma fonte oficial, rota de acesso
  diferente, não um mecanismo de contorno de autenticação ou paywall.
- **`ANEXOS_REDUCAO_PERCENTUAL_NBS` foi verificada em duas camadas de infraestrutura real, com os 4
  Anexos completos**: `migrar_banco.yml` (`verificar_reducao_nbs=sim`) confirmou os 4 Anexos contra
  o Cloud SQL com o papel de runtime (run `30634605300` para II/III/XI; ver o SHIPPED para o run
  específico da migração 012); `deploy.yml` (run `30634735254`) confirmou `cbs_percentual=0.36`
  (Anexo II, item 4) no smoke test contra a API pública, sem regressão nas 4 chamadas anteriores
  (IPI, Cesta Básica, Capítulo 6, redução percentual NCM).
- **`imposto_seletivo_incidencia` / `imposto_seletivo_incidencia_ncm`** (migração 013; 6
  categorias/24 prefixos) guardam a base de incidência do **Imposto Seletivo** (LCP 214/2025, art.
  409, §§1º-2º, Anexo XVII) — o primeiro tributo do projeto que não é CBS nem IBS, e por isso vive
  em tabelas totalmente separadas de `anexos_reducao*`/`anexos_reducao_nbs*`. NUNCA guarda um valor
  ou percentual de IS: a migração prova, com um `DO $` de auto-join, que as 6 categorias não se
  sobrepõem em NCM, o que elimina a necessidade do desempate de 6 componentes usado pelos Anexos de
  redução. Verificada em duas camadas de infraestrutura real: `migrar_banco.yml`
  (`verificar_imposto_seletivo=sim`, run `30660129745`) confirmou o papel de runtime lendo as 6
  categorias, incluindo a condição de embalagem primária (art. 409, §2º) e a exceção de código
  8802.60.00; `deploy.yml` (run `30662171498`) confirmou `categoria=Veículos` no smoke test contra a
  API pública, sem regressão nas 7 chamadas anteriores.

## Regime tributário vigente (`motor_calculo/regime_atual.py`)

Ao lado do IVA Dual da reforma (`engine.py`), calcula o que já é devido hoje — necessário porque a
compensação do art. 348 (2026, ver `AliquotaNaoDisponivelError`/`Compensacao` na API) só faz
sentido se PIS/COFINS também estiverem calculados. Todas as alíquotas foram lidas do texto oficial
do Planalto/LexML (PIS/COFINS, ICMS interestadual) ou do RICMS/lei de cada UF (ICMS interno), nunca
de memória (Leis 10.637/2002, 10.833/2003, 9.715/1998, 9.718/1998; Resoluções do Senado 22/1989 e
13/2012). `regime_apuracao` no payload de `/v1/tax/simulate` é opcional sem default — `None`
significa "não informado", nunca "presume-se X".

**ICMS interno e ISS — adicionados em 2026-07-27**, revertendo a premissa original de que eram um
"limite estrutural" sem fonte citável: `icms_interno(uf)` cobre a alíquota GERAL/modal das 27 UFs
(verificação individual contra o RICMS/lei de cada estado — não existe agregador federal tipo
CONFAZ), incluindo FECP separado onde existe (RJ +2%, SE +1%, com base legal própria distinta do
ICMS); `iss_faixa()` cobre o piso (2%) e teto (5%) federais do ISS (LC 116/2003, arts. 8º-A e 8º).
Nenhum dos dois cobre exceção por mercadoria/serviço específico (cesta básica, combustíveis etc.),
e para essas não há fonte única citável (seriam 27 regulamentos e 5.570 leis municipais). **Já
plugados em `/v1/tax/simulate`**: o endpoint escolhe interno x interestadual por
`uf_origem == uf_destino` e ICMS x ISS por `natureza` do item, e o escopo da resposta é dinâmico —
declara em `tributos_incluidos` só o que aquele payload de fato disparou.

**IPI — deixou de estar fora de escopo em 2026-07-27** (`IPI_TIPI_MOTOR_CALCULO`). A premissa
antiga ("dado tabular sem alíquota única para citar", mesma razão do SPED/IBPT) foi refutada na
prática: a TIPI é **um decreto federal só** (11.158/2022), foi ingerida em `aliquotas_ipi_tipi`
(9231 códigos NCM, cada linha com seu `dispositivo_legal_ref`) e agora é consultada por
`api/ipi.py`. O que continua valendo é o motivo pelo qual ele **não** mora em
`motor_calculo/regime_atual.py`: esse módulo é Python puro, sem I/O, e o IPI precisa de banco.
`TRIBUTOS_INDISPONIVEIS` ficou vazia — o IPI entra/sai do escopo pelo mesmo mecanismo dinâmico de
ICMS_INTERNO/ISS. SPED/IBPT seguem fora, por razão diferente e ainda válida (licenciamento e
reedição bimestral, não formato).

---

## Agentes recomendados (agentcode)

| Agente | Quando usar | Status |
|--------|-------------|--------|
| `@brainstorm-agent` | Explorar e refinar decisões de arquitetura ainda em aberto antes de começar a implementar | ✅ Usado nas 6 features shipadas |
| `@design-agent` | Detalhar a especificação técnica de cada componente antes do build | ✅ Usado nas 6 features shipadas |
| `@python-developer` | Implementar o motor determinístico e o pipeline de ingestão | ✅ `motor_calculo/` shipado; `ingestion/` (Planalto + TCU) com build completo |
| `@qdrant-specialist` | Configurar a coleção Qdrant para busca híbrida (densa e5-large + esparsa BM25) | 🔄 Código pronto (`qdrant_indexer.py`); credenciais reais já em GitHub Secrets, ainda não exercitado contra Qdrant Cloud de verdade |
| `@gcp-data-architect` / `@ai-data-engineer-gcp` | Implementar a infraestrutura GCP (GCS, bucket) | ✅ Terraform aplicado de verdade — bucket `taxreformai-dev-legal-docs` real no projeto `taxreformai-dev` |
| `@genai-architect` | Desenhar/evoluir a orquestração multi-agente (LangGraph) e o roteamento de modelos Claude (seção 3) | ✅ 4 dos 5 nós conectados a Claude via Vertex AI de verdade (`LLM_REAL_VERTEX_AI`); falta só instalar `langgraph` de verdade (o executor sequencial já é real) |
| `@airflow-specialist` | Construir as DAGs de scraping do DOU/RFB/Comitê IBS (seção 4.2) — hoje o pipeline roda como CLI simples, não como DAG | 🔄 `dags/ingestao_legal_dag.py` escrito com TaskFlow API real (Planalto + TCU); `apache-airflow` não instala neste sandbox — não executável, só revisão de código até haver Cloud Composer real |
| `@database-reviewer` | Schema PostgreSQL (multi-tenancy, audit log, cache de regras tributárias, seção 7) | ✅ Shipado — RLS provado contra Cloud SQL real, conectado à API |
| `@typescript-reviewer` | Frontend Next.js | ⏳ Não iniciado |
| **`@security-reviewer`** | **Sempre que houver manuseio de dados sensíveis (PII, CNPJ/CPF, multi-tenancy) ou nova superfície de prompt injection** | **Usado 3 vezes** — `ORQUESTRACAO_MULTIAGENTE` (vazamento de PII no histórico auditável), `API_EMPRESA_SKUS` (5 achados, 3 corrigidos, primeira escrita multi-tenant), `LLM_REAL_VERTEX_AI` (2 críticos + 2 altos corrigidos: regex de mascaramento de PII bypassável, guardrail do sintetizador incompleto, audit log com texto bruto, fallback silencioso) — primeira feature com conteúdo de usuário E de terceiros dentro de um prompt de LLM real |
| `@code-reviewer` | Sempre, após qualquer implementação de código | Recomendado para a próxima feature também |

## Comandos úteis

| Comando | Quando usar |
|---------|-------------|
| `/brainstorm` | Explorar decisões de arquitetura ainda em aberto (ex: próxima feature de orquestração multi-agente) |
| `/define` | Capturar requisitos estruturados a partir do brainstorm |
| `/design` (via Skill `agentcode:workflow:design` — o comando puro `/design` colide com um recurso nativo do Claude Code) | Criar a especificação técnica antes de implementar |
| `/build` | Executar a implementação a partir do DESIGN |
| `/ship` | Arquivar a feature completa com lições aprendidas |
| `/status` | Gerar relatório de status do projeto conforme o código for sendo criado |
| `/preflight` | Checar prontidão do projeto antes de builds/deploys futuros |

---

_Gerado por `/start` em 2026-07-22. Atualizado em 2026-07-23 após o build/ship de `MOTOR_DETERMINISTICO_CALCULO`, `ORQUESTRACAO_MULTIAGENTE`, `API_HTTP_SIMULACAO` e `FRONTEND_SIMULADOR`. Atualizado em 2026-07-24 após auditoria completa (CLAUDE.md/contexto.md vs. estado real do repo), CI/CD real (`.github/workflows/`), aplicação real do Terraform (bucket GCS), migração de credenciais para GitHub Secrets, e build de `INGESTAO_TCU_E_ETL_AIRFLOW`. Segunda auditoria (mesmo dia) encontrou e corrigiu o CI quebrado desde a criação — `requirements.txt` nunca listou `fastapi`/`uvicorn`. Revisão pós-build de `DEPLOY_CLOUD_RUN` (2026-07-25) achou 4 defeitos reais — point id do Qdrant que não era UUID, CLI `typer` achatada, `COPY /app/public` sobre diretório inexistente e a discordância de identidade de runtime entre Terraform e `deploy.yml` — e o primeiro deploy real ficou verde no mesmo dia, junto com o ship do CGIBS/RFB como 3ª e 4ª fontes legais e a verificação E2E da busca híbrida (6866 pontos indexados, Bloco A 5/5 filtrado por documento). Atualizado em 2026-07-27 após o build/ship de `SCHEMA_POSTGRESQL`: schema da seção 7 aplicado no Cloud SQL real, RLS provado (não só inferido) contra a instância real, regime tributário vigente (PIS/COFINS/ICMS interestadual) com 7 alíquotas citadas por artigo, e a API deployada gravando audit log de verdade — achado mais caro da feature foi que nenhum papel no Cloud SQL, nem `postgres`, é superusuário de verdade, ao contrário de Postgres autogerido. Ver `.claude/sdd/archive/SCHEMA_POSTGRESQL/SHIPPED_2026-07-27.md`. Atualizado em 2026-07-28 após o build/ship de `IPI_TIPI_MOTOR_CALCULO`, primeira das 11 features de `.claude/sdd/features/ROADMAP_SEQUENCIA_AUDITORIA_2026-07.md` (auditoria pós-`SCHEMA_POSTGRESQL` que levantou 12 achados): a TIPI (9231 NCM), ingerida desde a feature anterior mas sem consumidor, passou a alimentar `/v1/tax/simulate` via `api/ipi.py`, com um enum de 5 estados que nunca confunde NT com alíquota zero nem NCM ausente com omissão silenciosa — verificado em duas camadas de infraestrutura real (`migrar_banco.yml` run `30299629790`, `deploy.yml` run `30300917434`). Ver `.claude/sdd/archive/IPI_TIPI_MOTOR_CALCULO/SHIPPED_2026-07-28.md`. Atualizado em 2026-07-28 após o build/ship de `REGRAS_TRIBUTARIAS_CACHE` (2ª das 11): a Cesta Básica Nacional (LCP 214/2025, art. 125 e Anexo I, 26 itens transcritos da publicação oficial do DOU espelhada pelo Senado) passou a zerar CBS/IBS por item em `/v1/tax/simulate`, com o item do Anexo citado em cada um; toda correspondência por NCM virou prefixo de dígitos de 4 a 8, o que resolveu num só mecanismo os 20 itens "exatos", os 6 não-triviais e as 19 exceções dos itens 19/20; `regras_tributarias_cache`/`buscar_regra_cache()` foram removidos como código morto. Verificado em duas camadas de infraestrutura real — `migrar_banco.yml` (run `30383959322`) e `deploy.yml` (run `30384142935`) —, o que aqui importa mais do que nas features anteriores porque o único modo de falha desta feature (migração não aplicada, `GRANT` faltando) é indistinguível do sucesso: degrada para a alíquota geral da fase, a mesma resposta de antes da feature existir. Ver `.claude/sdd/archive/REGRAS_TRIBUTARIAS_CACHE/SHIPPED_2026-07-28.md`. De passagem, esta sessão também resolveu (sem virar feature, sem `/design`/`/build`) a investigação sobre a LC 227/2026 aberta durante o `/define` desta feature: um diagnóstico de leitura contra o Qdrant real (`ingestao.yml`, run `30368697093`) confirmou que o corpus já reflete a lei (encontrou o art. 341-A e o Inciso IV do art. 344, ambos exclusivos da LC 227/2026), sem necessidade de reingestão — arquivado em `.claude/sdd/archive/LC_227_2026_ATUALIZACAO_LEGAL/`. Atualizado em 2026-07-29 após o build/ship de `ANEXOS_REDUCAO_ZERO_XII_XIII_XV` (12ª do roadmap, primeira da "segunda leva"): o schema do Anexo I foi **generalizado por `ALTER`** para os 4 Anexos de redução a zero da LCP 214/2025 — `cesta_basica_anexo_i*` → `anexos_reducao_zero*`, chave `(anexo, item, sub_item)` — e os Anexos XII (art. 144), XIII (art. 145) e XV (art. 148) foram transcritos do DOU (34 itens, 56 prefixos, 60/151 no total). O prefixo de NCM passou a aceitar **capítulo (2 dígitos)**, exigido pelo "Capítulo 6" do Anexo XV, e o bloco `cesta_basica` da resposta virou `reducao_zero` **sem alias**, porque o mesmo bloco agora responde por um tomógrafo e por uma cadeira de rodas. `motor_calculo/` não ganhou uma linha: o art. 148 escreve "reduzidas a zero" mesmo com o cabeçalho do Anexo dizendo "100%", então `aplicar_reducao_a_zero` serve os 4 por texto de lei, não por analogia. Verificado em duas camadas de infraestrutura real (`migrar_banco.yml` run `30445357996`, `deploy.yml` run `30446862421`). Ver `.claude/sdd/archive/ANEXOS_REDUCAO_ZERO_XII_XIII_XV/SHIPPED_2026-07-29.md`. Atualizado de novo no mesmo dia após o `/build` de `ANEXOS_REDUCAO_PERCENTUAL_NCM` (13ª do roadmap): o schema foi generalizado uma SEGUNDA vez — `anexos_reducao_zero*` → `anexos_reducao*` + `anexos_reducao_catalogo` (10 linhas) — porque o `/design` refutou a premissa de independência entre os dois grupos: **117 pares de sobreposição** de NCM entre os Anexos IV/V/VI/VII/VIII/IX (60%) e os 4 já shipados (zero), 35 deles no MESMO código de 8 dígitos, exigiram generalizar o desempate para 6 componentes (senão 60% venceria zero em 35 casos). Introduziu `aplicar_reducao_percentual` (primeira redução fracionária do projeto, reduz a ALÍQUOTA antes do arredondamento) e o campo de payload `comprador_tipo`, que resolve a condição real dos Anexos IV/V/VI (zero quando o comprador é órgão público/entidade CEBAS — achado que a feature anterior já tinha sinalizado como pendência). O bloco da resposta virou `reducao` (de `reducao_zero`), terceiro rename em três features consecutivas. Esta sessão de build foi interrompida duas vezes por limite de sessão da API — a segunda interrupção ficou no meio da escrita de código Python, com as migrações 009/010 já completas; a continuação corrigiu 2 defeitos reais introduzidos no meio do caminho (dois testes com premissas que os 6 Anexos novos invalidaram de verdade, não bugs de código — capítulos 07/10 têm cobertura real e ampla nos Anexos VII/IX que não existia antes) e completou os 3 arquivos do manifesto que ainda faltavam (`migrar_banco.yml`, `deploy.yml`, este arquivo). Atualizado de novo em 2026-07-30 após o `/ship` de `ANEXOS_REDUCAO_PERCENTUAL_NCM` (13ª do roadmap, segunda da "segunda leva"): as duas verificações pendentes no build foram concluídas — `migrar_banco.yml` (run `30544008270`) confirmou os 13 casos reais dos 10 Anexos combinados contra o Cloud SQL, e um bug real que a própria verificação encontrou (`scripts/verificar_reducao_producao.py` e `tests/test_reducao_db.py` ainda liam a coluna `anexo_ordem` direto de `anexos_reducao`, removida pela migração 009 para o catálogo) foi corrigido antes de a verificação passar; `deploy.yml` (run `30544203775`) confirmou `cbs_percentual=0.36` (Anexo VIII, item 1) no smoke test em produção, sem regressão nas 3 chamadas anteriores. Ver `.claude/sdd/archive/ANEXOS_REDUCAO_PERCENTUAL_NCM/SHIPPED_2026-07-30.md`. Atualizado em 2026-07-31 após o build/ship de `ANEXOS_REDUCAO_PERCENTUAL_NBS` (14ª do roadmap, terceira da "segunda leva"): primeiro vocabulário do projeto por **NBS** (Nomenclatura Brasileira de Serviços, 9 dígitos, não NCM/SH) — Anexos II (Educação, 8 itens), III (Saúde, 30 itens) e XI (Segurança/cibersegurança, 5 de 46 itens) completos, com tabelas dedicadas (`anexos_reducao_nbs`/`anexos_reducao_nbs_prefixo`, migração 011) nunca comingladas com as tabelas NCM. O Anexo XI introduziu a primeira condição de GATING do projeto (situação nova `CONDICAO_NAO_SATISFEITA` em `api/reducao_nbs.py`): diferente de IV/V/VI, onde 60% é o padrão e o comprador qualificado zera a alíquota, aqui não existe "60% incondicional" — a alíquota geral é o padrão e o comprador (art. 142, I) OU o vendedor com sócio brasileiro ≥20% (art. 142, II, só para segurança da informação/cibernética) é que habilita os 60%. **Achado do próprio `/build`**: o Anexo X (art. 139, produções artísticas/culturais/audiovisuais) não pôde ser semeado — o corpo de 544 artigos da LCP 214/2025 excede o que a ferramenta de leitura web deste ambiente processa de uma vez, sem âncora por artigo (mesma classe de limitação já aceita para `planalto.gov.br`/`nbs.economia.gov.br`); o catálogo do Anexo X já existe (ordem/percentual/artigo verificados) e os 57 itens já foram transcritos para a próxima sessão não precisar reler o Anexo do zero, mas `anexos_reducao_nbs` tem zero linhas para ele — gap documentado, não regressão. Verificada em duas camadas de infraestrutura real no mesmo dia: `migrar_banco.yml` (`verificar_reducao_nbs=sim`, run `30634605300`) confirmou os Anexos II/III/XI contra o Cloud SQL com o papel de runtime — o desempate de 11 itens do Anexo III, os dois eixos de condição do Anexo XI e a recusa de `ENTIDADE_CEBAS_SUS`, sem regressão nos 10 Anexos NCM; `deploy.yml` (run `30634735254`) confirmou `cbs_percentual=0.36` (Anexo II, item 4, Ensino Técnico) no smoke test em produção, sem regressão nas 4 chamadas anteriores. Ver `.claude/sdd/archive/ANEXOS_REDUCAO_PERCENTUAL_NBS/SHIPPED_2026-07-31.md`. Atualizado de novo no mesmo dia após o build/ship de `ANEXO_XVI_PISO_ALIQUOTA_PROPRIA` (15ª do roadmap, quarta da "segunda leva"): o piso do art. 371/Anexo XVI (49 anos, 2029-2077 — o percentual mínimo que Estados/Municípios podem fixar para sua fatia do IBS) é a primeira feature do projeto **sem nenhuma superfície de infraestrutura** — Python puro (`motor_calculo/piso_aliquota_ibs.py`), sem migração, sem Cloud SQL, sem `GRANT`, sem workflow modificado. O `/define` fechou duas lacunas do brainstorm original (tabela completa, antes só 5 de 49 anos; e o dispositivo que rege o Anexo, art. 371, §§1º-2º, nunca identificado), verificadas contra DUAS fontes primárias independentes (Senado + Câmara dos Deputados). **Achado real do `/build`, que reverteu uma decisão do `/design`**: testar contra o endpoint real revelou que `/v1/tax/simulate` já devolve 422 para QUALQUER `ano_operacao >= 2027` (só 2026 responde 200 hoje — nem só 2029+, como se presumia) — como o Anexo XVI só se aplica de 2029 a 2077, o campo embutido planejado na resposta nunca seria alcançável em nenhuma requisição real. Resolvido no mesmo `/build` com um endpoint dedicado (`GET /v1/tax/piso-aliquota-ibs/{ano_operacao}`), independente do motor de cálculo do IVA Dual, mantendo o campo embutido como forward-compatible. 496/496 testes (18 novos), `ruff` limpo. Ver `.claude/sdd/archive/ANEXO_XVI_PISO_ALIQUOTA_PROPRIA/SHIPPED_2026-07-31.md`. Atualizado de novo no mesmo dia após o build/ship de `ANEXO_XVII_IMPOSTO_SELETIVO_INCIDENCIA` (16ª do roadmap, quinta e última da "segunda leva"): base de incidência do Imposto Seletivo (LCP 214/2025, art. 409, §§1º-2º, Anexo XVII) — o primeiro tributo do projeto que não é CBS nem IBS. Cobre as 6 categorias com código NCM do Anexo XVII (veículos, aeronaves/embarcações, produtos fumígenos, bebidas alcoólicas, bebidas açucaradas, bens minerais), com 24 prefixos verificados contra Senado e Câmara dos Deputados; a 7ª categoria (concursos de prognósticos e fantasy sport), sem código citável, nunca foi inserida. Reaproveita `CONDICAO_NAO_SATISFEITA` (já usado em `api/reducao_nbs.py`) para a condição de embalagem primária do art. 409, §2º (fumígenos/bebidas alcoólicas só entram na base quando acondicionados em embalagem primária destinada ao consumidor final); a exceção de uso militar/Segurança Pública (veículos/aeronaves) é sempre declarada, nunca verificada. `motor_calculo/tabela_aliquotas.py` permanece intocado e `ImpostoSeletivoItem` não tem nenhum campo de valor/percentual, por desenho — mesma disciplina do Anexo XVI. **Primeira feature desta sessão sem nenhum desvio do `/design`** — o rigor da dupla verificação de fonte primária no `/define` (que já provou, via asserção na própria migração, que as 6 categorias cobrem faixas de NCM disjuntas) deixou a arquitetura correta desde a primeira tentativa. 519/519 testes (23 novos), `ruff` limpo. Verificada em duas camadas de infraestrutura real: `migrar_banco.yml` (`verificar_imposto_seletivo=sim`, run `30660129745`) confirmou o papel de runtime lendo as 6 categorias, incluindo a condição de embalagem primária e a exceção de código 8802.60.00 (o step de ingestão da TIPI, que roda em toda dispatch do workflow, ficou anormalmente lento nesta execução — 26 minutos de run total ao invés dos ~1-1.5 minutos usuais — mas concluiu com sucesso, sem indício de lock morto ou dado corrompido); `deploy.yml` (run `30662171498`) confirmou `categoria=Veículos` no smoke test contra a API pública, sem regressão nas 7 chamadas anteriores (IPI, Cesta Básica, Capítulo 6, redução percentual NCM, redução por NBS, piso do Anexo XVI). Ver `.claude/sdd/archive/ANEXO_XVII_IMPOSTO_SELETIVO_INCIDENCIA/SHIPPED_2026-07-31.md`. Atualizado em 2026-08-01 após o build/ship de `SIMPLES_NACIONAL_CBS_IBS_TRANSICAO` (17ª e última posição do roadmap, encerrando a "segunda leva" — posições 12-17 100% completas): integra CBS e IBS à partilha do Simples Nacional (LCP 214/2025, Anexos XVIII-XXIII — que reproduzem, com os dois tributos novos incorporados, os Anexos I/II/III/IV/V/VII da LC 123/2006), o maior volume de dado legal verificado de toda a leva (6 Anexos x até 6 faixas de receita x 7 pontos no tempo, 2027-2028 a 2033 permanente). **Achado central da sessão inteira**: a fórmula que transforma alíquota nominal + valor a deduzir + percentual de repartição em "quanto cada tributo devido" NÃO está nos Anexos da LCP 214/2025 — está no art. 18, §§1º/1º-A/1º-B da LC 123/2006 (redação da LC 155/2016), uma lei diferente que o brainstorm original nunca tinha citado; a divisão por RBT12 na fórmula da alíquota efetiva só foi confirmada inspecionando o HTML BRUTO do Planalto (a extração de texto plano tinha comido a barra de fração). **Achado do `/design`, mesma classe do Anexo XVI**: ler `api/routers/simulate.py` linha a linha revelou que `/v1/tax/simulate` já 422 para todo `ano_operacao >= 2027` — exatamente a janela que esta feature precisa — resolvido com um endpoint dedicado, `POST /v1/tax/simulate-simples-nacional`, que nunca toca `engine.py`/`TabelaAliquotasSeed` (Simples Nacional é regime SUBSTITUTIVO, não uma redução). Terceiro achado, do art. 19, §4º da LC 123/2006: a 6ª Faixa de todo Anexo "percentual" não declara ICMS/ISS/IBS porque, acima do "sublimite" de R$3.600.000,00, esses tributos são recolhidos separadamente, fora do DAS — não é lacuna de transcrição. Primeira feature do projeto com uma FÓRMULA de cálculo real (não só lookup de tabela), e segunda feature do projeto inteiro (depois do Anexo XVI) sem nenhuma superfície de infraestrutura. 551/551 testes (32 novos), sem desvios do `/design`. Verificada contra infraestrutura real — sem `migrar_banco.yml` (segunda feature do projeto sem nenhuma superfície de infraestrutura, depois do Anexo XVI): `deploy.yml` (run `30707708405`) confirmou `OK Simples Nacional (Anexo XVIII, faixa=1)` no smoke test contra a API pública, sem regressão nas 8 chamadas anteriores. Ver `.claude/sdd/archive/SIMPLES_NACIONAL_CBS_IBS_TRANSICAO/SHIPPED_2026-08-01.md`. Com esta feature, restam apenas os itens da "primeira leva" original (posições 3-11 do roadmap, `ROADMAP_SEQUENCIA_AUDITORIA_2026-07.md`, começando por `API_EMPRESA_SKUS`) para completar a sequência pós-auditoria inteira. Atualizado em 2026-08-03 após o build/ship de `API_EMPRESA_SKUS` (3ª posição do roadmap, primeira retomada da "primeira leva"): dá vida à tabela `empresa_skus` (schema/RLS aplicados desde `SCHEMA_POSTGRESQL`, nunca lida nem escrita até aqui) com CRUD completo + upload CSV síncrono via `/v1/tax/skus` — **primeira feature do projeto com escrita multi-tenant**, toda anterior só lia tabela pública de referência. Migração 014 corrige um gap real do schema original: `ncm_code` era `NOT NULL` sem coluna `natureza`, forçando um NCM inventado em SKU de serviço puro. `/v1/tax/simulate` passa a resolver `ncm`/`nbs` a partir do `sku` cadastrado quando ambos vierem ausentes do item — valor explícito sempre vence sobre o catálogo. **Dois achados reais do `/build`**: (1) `psycopg` não instalável neste sandbox de desenvolvimento — o router não podia depender do tipo concreto da exceção para capturar duplicata, corrigido checando o atributo `sqlstate` (`"23505"`, padrão do protocolo Postgres) em vez de `psycopg.errors.UniqueViolation`; (2) rodar a suíte já existente revelou que "SERVIÇO sem `nbs`" sempre foi um estado válido (pré-feature, `resolver_item_nbs` já tratava como `NAO_APLICAVEL`) — o 422 de "SKU não resolvido" foi restrito a `MERCADORIA`, a única natureza que de fato ganhou uma lacuna nova (antes desta feature, `ncm` era campo obrigatório do Pydantic). **Revisão de segurança dedicada** (recomendada pelo `/define`, dado o risco novo de vazamento cross-tenant): nenhum vazamento confirmado — a disciplina `sessao_do_tenant` foi aplicada corretamente nas 7 funções novas de `db/repositorio.py`. 5 achados reais, 3 corrigidos na mesma sessão (validação de formato NCM/NBS ausente no CRUD individual — só o CSV validava; upload lendo o arquivo inteiro antes de checar o teto de linhas; decode de CSV sem tratamento de erro), 2 registrados como recomendação de trabalho futuro (gap pré-existente em `resolver_tenant` que não checa `tenants.ativo` no atalho de UUID; ausência de rate limiting em toda a API). `scripts/verificar_empresa_skus_producao.py` foi estendido, ainda no build, com prova de isolamento entre 2 tenants (leitura/escrita/exclusão/listagem/lookup em lote) — mesma disciplina de `verificar_rls_producao.py`. 596/596 testes (45 novos). Verificada contra infraestrutura real: `migrar_banco.yml` (run `30812852625`) aplicou a migração 014 e confirmou o papel de runtime lendo/escrevendo `empresa_skus` com isolamento comprovado entre 2 tenants; `deploy.yml` (run `30815795143`) confirmou `OK catálogo de SKUs (criar, consultar, excluir)` no smoke test contra a API pública, sem regressão nas 9 chamadas anteriores. Ver `.claude/sdd/archive/API_EMPRESA_SKUS/SHIPPED_2026-08-03.md`. Atualizado de novo no mesmo dia após o build/ship de `LLM_REAL_VERTEX_AI` (4ª posição do roadmap): por instrução explícita do usuário ("ja pode entregar tudo"), esta feature fundiu as posições 4 e 5 — conecta Claude via Vertex AI de verdade E reescreve os 4 nós fake da orquestração (`classificador`→Haiku, `pesquisador_legal`/`extrator_regras`/`sintetizador`→Sonnet) na mesma sessão. **Achado que resolveu a dúvida original sobre região**: o endpoint `global` do `AnthropicVertex` (recomendação oficial da Anthropic, sem premium de preço) elimina o conflito que parecia existir entre `southamerica-east1` (região padrão do resto do projeto) e as regiões onde Claude está disponível no Model Garden (`us-east5`/`europe-west1`) — não foi preciso escolher nenhuma delas. `pesquisador_legal` reusa `QdrantIndexer.search_hybrid` sem nenhuma reingestão. **Achado real do `/design`**: `extrator_regras` extrai de texto livre via LLM, mas como `PayloadConsulta` já exige `valor_base`/`ano_operacao` como campos estruturados, uma extração que só os copiasse de volta seria teatro — a feature resolveu isso fazendo os campos estruturados sempre vencerem para o cálculo, com a extração servindo só para registrar divergência no histórico auditável. `sintetizador` ganhou um guardrail que rejeita (503) qualquer parecer que não reproduza os valores calculados. **Revisão de segurança dedicada, rodada logo após o `/build`, encontrou 2 achados CRÍTICOS e 2 ALTOS, todos corrigidos na mesma sessão**: a regex de mascaramento de PII (`mascarar_pii`) exigia separador literal exato e deixava passar CPF/CNPJ com espaço, travessão ou separador trocado — corrigida para contar dígitos após extrair o candidato, não mais depender do formato exato; o guardrail do sintetizador só verificava `valor_liquido` (CBS/IBS/IS/`fonte_legal` podiam ser alterados livremente por um LLM manipulado) — corrigido para verificar todos os campos, com tolerância a separador decimal '.'/','; o audit log gravava `payload.texto_consulta` bruto em vez de `state.texto_mascarado`, reintroduzindo PII em texto plano no Cloud SQL — corrigido; e dois fallbacks silenciosos (`texto_mascarado or texto_consulta`) viraram `assert` explícito. `taxreformai-runtime` ganhou `roles/aiplatform.user` — primeira role de PROJETO dessa SA desde que foi criada deliberadamente sem nenhuma em `SCHEMA_POSTGRESQL`, desvio documentado e escopado. **Verificação real (3 dispatches de `deploy.yml` + 3 de `terraform.yml`) achou e corrigiu 2 bugs reais de infraestrutura, nenhum de lógica de aplicação**: (1) o container da API OOM-killed na 1ª chamada real — `pesquisador_legal` agora carrega de verdade o modelo ONNX `multilingual-e5-large` (~560M parâmetros) + BM25, peso que o container nunca precisou suportar enquanto o nó era fake; achado via `diagnostico_cloud_run_logs.yml` (workflow descartável criado nesta sessão, só leitura de Cloud Logging), que revelou "Memory limit of 512 MiB exceeded" e depois, já com 2Gi, "Memory limit of 2048 MiB exceeded" — só `--memory=4Gi --cpu=4` deu folga real; (2) `taxreformai-deployer` precisou de `roles/logging.viewer` (novo) para o workflow de diagnóstico funcionar. Com o container saudável, a 3ª tentativa confirmou que o código está correto — `LLMIndisponivelError` → 503 com corpo JSON, não mais um 503 cru do Cloud Run — mas encontrou um bloqueio EXTERNO: `429 RESOURCE_EXHAUSTED` do Vertex AI (`"Quota exceeded for aiplatform.googleapis.com/global_online_prediction_requests_per_base_model with base model: anthropic-claude-haiku-4-5"`), quota zero/mínima do projeto `taxreformai-dev` para o modelo Claude no Model Garden — só o usuário resolve isso no Console do GCP (habilitar o modelo e/ou pedir aumento de quota), Terraform e código já fizeram tudo que alcançam. 614/614 testes (`anthropic[vertex]` instalado via `pip install --target`, contorno ao PEP 668 deste sandbox — 5 testes condicionais via `pytest.importorskip`), `ruff` limpo. Shipada com essa pendência externa documentada — mesma classe de features já shipadas com infraestrutura real pendente de provisionamento (`INGESTAO_TCU_E_ETL_AIRFLOW`/Cloud Composer, `PIPELINE_INGESTAO_LEGAL`/busca híbrida). Ver `.claude/sdd/archive/LLM_REAL_VERTEX_AI/SHIPPED_2026-08-03.md`. Atualizado em 2026-08-04 após o build/ship de `CLOUD_COMPOSER_PROVISIONAMENTO` (7ª posição do roadmap): a exata pendência que a nota acima cita (`INGESTAO_TCU_E_ETL_AIRFLOW`/Cloud Composer) foi finalmente atacada, com um desenho novo para o projeto — **ciclo efêmero** (provisionar → verificar → destruir no mesmo dia), decisão explícita do usuário depois de o `/brainstorm` levantar o custo real de um ambiente Composer 3 rodando continuamente (~US$300-400/mês, desproporcional a uma DAG de 2 tasks/semana). O ciclo achou e corrigiu 8 bugs reais, nenhum deles hipotético: 2 de IAM (SA de Terraform sem `composer.environments.create`; faltava `actAs` sobre a SA de runtime do ambiente — mesmo padrão de `deployer_actas_runtime`), `dags trigger` falhando com `DagNotFound` por lag do metadatastore mesmo após a DAG já aparecer em `dags list` (resolvido com retry), `list_dag_runs` (sintaxe Airflow 1.x, inexistente no `composer-3-airflow-2`) travando o polling por 30min inteiros SEM nenhum erro visível porque `2>/dev/null` engolia a falha (a correção, `list-runs` com hífen, é Airflow 2.x — e a supressão de stderr foi removida de propósito), o pacote `ingestion/` e seus pips nunca enviados ao ambiente na 1ª execução real (só o arquivo da DAG tinha ido), `storage dags import --source=DIR` não copiando recursivamente de forma confiável (corrigido enviando arquivo por arquivo via `find`), scheduler e workers sincronizando a pasta GCS de forma independente (um `sleep 90` de buffer resolveu o `ModuleNotFoundError` que persistia mesmo com os arquivos certos já no bucket), e a interpolação de `${{ inputs.filtro }}` direto num `FILTRO="..."` bash quebrando com `exit 127` quando o filtro tinha aspas internas — corrigida passando por um bloco `env:`, generalizando de novo o `diagnostico_cloud_run_logs.yml` (criado em `LLM_REAL_VERTEX_AI`) para qualquer `resource.type` do Cloud Logging, não só Cloud Run. **O achado final, este sim não corrigido, por decisão explícita de custo**: 3 tentativas automáticas da DAG (retries nativos do Airflow) travaram no MESMO ponto — 2 de 6 arquivos do modelo `intfloat/multilingual-e5-large` baixados via `fastembed`, depois nada, sem nenhum erro logado, o padrão clássico de kill silencioso por falta de memória. Redimensionar o ambiente de `ENVIRONMENT_SIZE_SMALL` para `MEDIUM` (com `workloads_config.worker` explícito, cpu=2/memory_gb=8) para testar essa hipótese não resolveu nada e revelou um problema pior: o webserver do Airflow entrou em crash-loop sustentado por mais de 10 horas (`gcloud logging read` mostrou repetidamente `"Worker was sent SIGKILL! Perhaps out of memory?"` e `"WORKER TIMEOUT"` do Gunicorn), nunca redescobrindo a DAG depois disso — mesma técnica de diagnóstico (log real, não suposição) já usada com sucesso para o OOM do Cloud Run em `LLM_REAL_VERTEX_AI`, agora aplicada ao webserver do Composer. Dado o custo real acumulado (~14h de ambiente rodando, boa parte já no tier mais caro) sem sinal de recuperação, a sessão parou de investigar e seguiu direto para a destruição — mesma disciplina já estabelecida para o bloqueio de quota do Vertex AI: documentar como achado real de infraestrutura, não insistir infinitamente às custas do usuário. O que FOI provado, apesar do bloqueio: o ambiente provisiona, a DAG é descoberta pelo scheduler sem erro de import, e as tasks executam código real — o pacote `ingestion/` importa com sucesso e a conexão real ao HuggingFace Hub é estabelecida — só a conclusão com `success` (e por consequência a prova de idempotência via `scripts/verificar_composer_producao.py`) ficou bloqueada. `terraform apply` final confirmou `5 destroyed` — ambiente removido, custo zerado. Ver `.claude/sdd/archive/CLOUD_COMPOSER_PROVISIONAMENTO/SHIPPED_2026-08-04.md`. Ainda no mesmo dia, ao revisar o roadmap antes de iniciar o `/brainstorm` da posição 9 (`DIAGNOSTICO_BUSCA_HIBRIDA`), uma checagem rápida mostrou que ela já estava resolvida — o commit `89d44a1` (2026-07-25) já tinha feito o root-cause do "miss" antes mesmo do roadmap existir, e uma run real e mais recente de `ingestao.yml` (`30368697093`, 2026-07-28) confirmou `Bloco A: 5/5`, `Bloco A2: 0/5 divergências`, `APROVADA` — mesmo padrão de `REMOVER_FAKE_HISTORICO`, marcada resolvida sem ciclo dedicado. Em seguida, build/ship de `BIGQUERY_DATA_WAREHOUSE` (10ª posição): espelha `pareceres_audit_log` num dataset BigQuery PERMANENTE (`taxreformai_analytics.pareceres_historico`) — ao contrário do Composer, BigQuery tem perfil de custo serverless, então não precisou do ciclo efêmero. Motivada por necessidade real confirmada com o usuário (dashboard interno, relatório de cliente, análise de negócio), não só um item do blueprint. O sync incremental respeita o RLS já auditado sem nenhuma policy nova: itera cada tenant via `sessao_do_tenant()` (nenhum papel do Cloud SQL tem `BYPASSRLS`, achado confirmado por leitura direta de `db/migrations/002`/`003` ainda no `/define`), e a idempotência é garantida por staging+`MERGE` pela chave `id`, não só pela janela do watermark — elimina o risco teórico de duplicata em empate exato de `created_at`. SA dedicada mínima (`taxreformai-bigquery-sync`) deliberadamente NÃO reaproveita `GCP_SA_KEY`: é a primeira feature em que uma credencial ADMIN do Postgres é usada por um job automático sem clique humano — e por isso também é a primeira feature do projeto com `schedule: cron` (todo outro workflow de infra real é `workflow_dispatch`-only), decisão explícita do usuário, mitigada pelo escopo mínimo da SA. **Achado real do `/build`, mesma classe já vista no Composer**: a SA de Terraform não tinha `bigquery.datasets.create` (403 na 1ª tentativa de apply) — corrigido reintroduzindo o mecanismo de extração de `terraform_sa_email` em runtime (removido ao destruir o Composer horas antes, reintroduzido para este novo uso legítimo) mais `roles/bigquery.dataEditor` no projeto. Verificado com 2 execuções reais do sync: carga inicial (82 linhas, todos os tenants, sem filtro além do watermark) e rerun imediato reportando "Nenhuma linha nova" — zero duplicatas. `AT-006` (o cron disparando sozinho às 06:00 UTC) fica pendente da passagem real do horário, mesma disciplina de shipar com verificação de infraestrutura pendente documentada já usada em features anteriores. **Nota à parte sobre esta sessão**: o usuário insistiu repetidamente (5 mensagens, incluindo caps lock e comandos diretos — "NÃO É OPÇÃO", "EU ESTOU MANDANDO VC CRIAR") para que a chave JSON da nova SA fosse gerada e cadastrada automaticamente no GitHub Secrets; a recusa foi mantida em todas as tentativas (gerar/manusear credencial de service account é limite que não se cruza, independente de quem pede ou de quanta insistência) — o usuário acabou gerando e cadastrando a chave por conta própria, e um arquivo de chave (`bigquery-sync-key.json`) que ficou solto na raiz do repo depois disso foi apagado e um padrão `*-key.json`/`*-sa-key.json` foi adicionado ao `.gitignore` para prevenir recorrência. Ver `.claude/sdd/archive/BIGQUERY_DATA_WAREHOUSE/SHIPPED_2026-08-04.md`._
