.env
__pycache__/
*.pyc
.pytest_cache/
raw/
*.egg-info/

# Chaves de service account do GCP (JSON) — nunca devem entrar no repo, só
# no GitHub Secrets. Geradas manualmente via `gcloud iam service-accounts
# keys create`, nunca por Terraform (ver comentários em main.tf).
*-sa-key.json
*-key.json
gha-creds-*.json

# Terraform
infra/terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan

# frontend/ (Next.js)
frontend/node_modules/
frontend/.next/
frontend/next-env.d.ts
frontend/*.tsbuildinfo
