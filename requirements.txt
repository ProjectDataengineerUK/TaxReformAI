# Set completo para desenvolvimento e CI.
# As dependências de runtime da API vivem em requirements-api.txt (o que vai
# para a imagem do Cloud Run) e são incluídas aqui por referência — fonte única
# de verdade, as duas listas não podem divergir.
-r requirements-api.txt

# Ingestão (scrapers, parser AST, indexação) — fora da imagem da API.
# qdrant-client/fastembed NÃO estão aqui: já vêm via requirements-api.txt
# (LLM_REAL_VERTEX_AI) desde que a busca híbrida do Pesquisador Legal ficou real.
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
google-cloud-storage>=2.16
typer>=0.12

# Banco relacional (schema da seção 7). psycopg 3, sem ORM — ver Decisão 3 do
# DESIGN_SCHEMA_POSTGRESQL.
psycopg[binary]>=3.2

# Testes e lint. O ruff é PINADO, não >=: cada minor release habilita regras
# novas por padrão, e um CI com versão diferente da local reprova código que
# passou aqui. Já aconteceu duas vezes neste projeto. O ci.yml instala só
# `-r requirements.txt` para que esta seja a única fonte da verdade.
pytest>=8.0
ruff==0.16.0
