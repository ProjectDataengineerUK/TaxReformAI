# Set completo para desenvolvimento e CI.
# As dependências de runtime da API vivem em requirements-api.txt (o que vai
# para a imagem do Cloud Run) e são incluídas aqui por referência — fonte única
# de verdade, as duas listas não podem divergir.
-r requirements-api.txt

# Ingestão (scrapers, parser AST, embedding, indexação) — fora da imagem da API.
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
qdrant-client>=1.10
google-cloud-storage>=2.16
fastembed>=0.3
typer>=0.12

# Testes
pytest>=8.0
