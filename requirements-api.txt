# Dependências de runtime do serviço da API (api/) — usado por api/Dockerfile.
#
# Mantenha esta lista mínima: é o que vai para a imagem de produção do Cloud Run.
# Rastreado a partir dos imports reais de api/, orquestracao/ e motor_calculo/.
#
# requirements.txt inclui este arquivo via `-r`, então as duas listas não podem
# divergir: adicionar algo aqui é suficiente para o CI também instalar.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7

# LLM_REAL_VERTEX_AI: até esta feature, o acoplamento com ingestion/ era só
# ingestion.chunking.chunk_models.Chunk (Pydantic puro) — nenhuma dependência
# pesada de ingestão era necessária aqui, porque `pesquisador_legal` era fake.
# Agora `orquestracao/nos/pesquisador_legal.py` faz busca híbrida REAL a cada
# `/v1/tax/query`, então a imagem da API precisa do client Qdrant e do
# embedder — a mesma exclusão documentada acima deixou de valer.
anthropic[vertex]>=0.40
qdrant-client>=1.10
fastembed>=0.3

# API_EMPRESA_SKUS: upload de SKUs em CSV (multipart/form-data) — FastAPI
# exige este pacote para aceitar UploadFile/File(...). Já era instalado como
# dependência transitiva de algum pacote acima, mas listar explicitamente
# evita que um bump futuro o remova em silêncio (mesma disciplina de manter
# esta lista rastreável a partir dos imports reais).
python-multipart>=0.0.9

# Audit log (seção 7). Pool, não conexão avulsa: o serviço é atingido a cada
# /simulate e /query, e abrir conexão por requisição contra Cloud SQL soma
# latência real. [binary] traz o driver C pré-compilado — a imagem
# (python:3.12-slim) não tem toolchain de build para compilar a versão pura.
psycopg[binary]>=3.2
psycopg-pool>=3.2

# FILA_ASSINCRONA_CELERY_REDIS: staging do arquivo de upload (GCS), criação
# da Cloud Task e verificação do token OIDC que ela anexa na chamada de
# volta. Primeira vez que a imagem da API fala com GCS/Cloud Tasks — antes
# só o lado de ingestão (fora da imagem da API) usava google-cloud-storage.
google-cloud-storage>=2.16
google-cloud-tasks>=2.16
google-auth>=2.30

# PAINEL_OBSERVABILIDADE: observabilidade/status.py chama Frontend/Qdrant via
# HTTP direto (httpx já era transitiva do anthropic SDK, mas listada aqui
# pela mesma disciplina de rastreabilidade explícita já documentada acima);
# observabilidade/scorecard.yaml é lido por api/routers/observabilidade.py
# via PyYAML — nenhum dos dois pacotes tinha consumidor real na imagem antes.
httpx>=0.27
PyYAML>=6.0
